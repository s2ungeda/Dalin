unit FKipOrderConfig;

interface

uses
  Winapi.Windows, Winapi.Messages, System.SysUtils, System.Variants, System.Classes, Vcl.Graphics,
  Vcl.Controls, Vcl.Forms, Vcl.Dialogs, Vcl.StdCtrls, Vcl.Mask, Vcl.ExtCtrls,

  UOrderParams
  ;

type
  TFrmKipOrderConfig = class(TForm)
    Button1: TButton;
    Button2: TButton;
    GroupBox1: TGroupBox;
    edtRetryCount: TLabeledEdit;
    edtRetryInterval: TLabeledEdit;
    LabeledEdit1: TLabeledEdit;
    edtOrderInterval: TLabeledEdit;
    Label1: TLabel;
    Label2: TLabel;
    ckOrdInterval: TCheckBox;
    ckNotify: TCheckBox;
    Label3: TLabel;
    procedure Button2Click(Sender: TObject);
    procedure Button1Click(Sender: TObject);
  private
    FConfig: TKipConfig;
    { Private declarations }
  public
    { Public declarations }

    function Open( aCfg : TKipConfig ) : boolean;

    property Config : TKipConfig read FConfig;
  end;

var
  FrmKipOrderConfig: TFrmKipOrderConfig;

implementation

{$R *.dfm}

{ TFrmKipOrderConfig }

procedure TFrmKipOrderConfig.Button1Click(Sender: TObject);
begin

  TryStrToInt( edtRetryCount.Text,  FConfig.ReTryCount );
  TryStrToInt( edtRetryInterval.Text, FConfig.ReTryInterval );
  TryStrToInt( edtOrderInterval.Text, FConfig.OrderInterval );

  if FConfig.OrderInterval < 10 then
  begin
    ShowMessage('진입청산 인터벌 값은 10 보다 커야 함');
    edtOrderInterval.SetFocus;
    ModalResult := mrNone;
    Exit;
  end;

  if FConfig.ReTryInterval <= 10 then
  begin
    ShowMessage('재진입 인터벌 값은 10 보다 커야 함');
    edtRetryInterval.SetFocus;
    ModalResult := mrNone;
    Exit;
  end;

  if FConfig.ReTryCount <= 0 then
  begin
    ShowMessage('재진입 횟수는 0 보다 커야 함');
    edtRetryCount.SetFocus;
    ModalResult := mrNone;
    Exit;
  end;

  FConfig.UseOrdInterval  := ckOrdInterval.Checked;
  FConfig.UseNotify       := ckNotify.Checked;

  ModalResult := mrOK;
end;

procedure TFrmKipOrderConfig.Button2Click(Sender: TObject);
begin
  ModalResult := mrCancel;
end;

function TFrmKipOrderConfig.Open( aCfg : TKipConfig ): boolean;
begin

  edtRetryCount.Text    := IntToStr( aCfg.ReTryCount );
  edtRetryInterval.Text := IntTostr( aCfg.ReTryInterval );
  edtOrderInterval.Text := IntToStr( aCfg.OrderInterval );

  ckOrdInterval.Checked := FConfig.UseOrdInterval;
  ckNotify.Checked      := FConfig.UseNotify;

  Result :=  (ShowModal = mrOK);
end;

end.
