unit USPOrders;

interface

uses
  System.Classes, System.SysUtils, System.DateUtils, vcl.ExtCtrls,  Windows,

  USymbols, UOrders, UAccounts, UDistributor,

  USymbolCore, UQuoteBroker, UQuoteMerge,

  USPItems, UApiTypes, UTypes

  ;

type

  TSPSymbolArray  = array [TSPType] of TSymbol;
  TMergedQuoteArray = array [TSPType] of TMergedQuote;
  TSPAccountArray = array [TSPType] of TAccount;
  TSPPositionArray = array [TSPType] of TSPPosition;
  TSPAssetArray = array [TSPType] of TAsset;
  TSPArray  = array [TAutoOrderType] of double;

  TSPOrdUnitArray = array [TAutoOrderType] of TSpOrderUnit;
  TReadyArray = array [TAutoOrderType] of boolean;

  TQuoteNotifiy = procedure ( Sender : TObject; sType : TSPType ) of object;

  TSPOrder = class( TCollectionItem )
  private
    FAccounts: TSPAccountArray;
    FSymbols: TSPSymbolArray;
    FParam: TSPParam;
    FRun: boolean;
    FSPValue: TSPArray;
    FName: string;
    FMgQuotes: TMergedQuoteArray;
    FOnSPNotify: TTextNotifyEvent;
    FPositions: TSPPositionArray;

    FOrderUnit: TSPOrdUnitArray;
    FReady: TReadyArray;
    FCompPrice: TSPArray;
    FOrderPrice: TSPArray;
    FBasePrice: TSPArray;
    FTargetPrice: TSPArray;
    FBaseVolume : TSPArray;
    FModifyList: TList;
    // 주 주문의 체결량이 섭 주문수량의 몇%인지 확인하기 위해 섭 주문의 주문수량을 저장한다.
    // 주문수량은 매번 달라질수 있음
    FLastOrdQty : TSPArray;

    FTimer  : array [TAutoOrderType] of TTimer;


    FModifyOrderList : array [TAutoOrderType] of TModifyOrderList;
    FAssets: TSPAssetArray;
    FBasePosQty: double;
    FOnQuoteNotify: TQuoteNotifiy;
    FNo: integer;
    FOnSPNotify2: TTextNotifyEvent;

    procedure DoLog( stLog : string; bShow : boolean = false );
//    procedure QuoteProc(Sender, Receiver: TObject; DataID: Integer;
//                    DataObj: TObject; EventID: TDistributorID);

    procedure TradeProc(Sender, Receiver: TObject; DataID: Integer;
                    DataObj: TObject; EventID: TDistributorID);
    procedure OnOrder( aOrder : TOrder; iEventID : integer );
    procedure DoMustOrder( ptType : TPositionType );
    procedure OnFill( aOrder : TOrder );

    procedure OnDelayTimer(Sender:TObject);
    procedure OnMustOrderTimer(Sender:TObject);
    procedure OnQuote( aSymbol : TSymbol );

    function IsRun : boolean;

    procedure CalcSPValue;
    procedure SetParam(const Value: TSPParam);

    procedure OnWathcMajorOrder;

    procedure OnPave(bEntry: boolean);
    procedure OnEntry;
    procedure OnWatchEntryOrder;
    function CheckEntryLimit(const dOrderQty : double ) : boolean;

    procedure OnLiquid;
    procedure OnWatchLiquidOrder;
    function CheckLiquidLimit( const dOrderQty : double ): boolean;

    function GetMajorBasePrice( bEntry : boolean; bCheckVol : boolean ): TMarketDepth;
    function GetSubCompPrice(bEntry : boolean ) : double;
    function GetSubEntryPrice(dTargetPrice, dBasePrice: double; var dEntryPrice : double): boolean;
    function GetSubLiquidPrice(dTargetPrice, dBasePrice: double; var dLiqPrice: double): boolean;

    function DoOrder( aAcnt : TAccount; aSymbol : TSymbol; iSide : integer;
        dPrice : double; dOrderVol : double; bEntry : boolean ) : TOrder;
    function SetPrice(bEntry: boolean): boolean;
    function SetPrice2(bEntry: boolean): boolean;

    procedure OnOrderUnitLog (Sender: TObject; Value: String);
    procedure CheckAutoPutOrder(aType : TAutoOrderType; aOrder: TOrder);
    procedure SetReady(aType: TAutoOrderType; bValue: boolean);

    procedure OnModifyOrderLog(Sender: TObject; Value: String);

    function GetOrderQty( aType : TAutoOrderType ) : double;
    function GetTradeRate: double;
    function GetTradePL  : double;
    function GetLastPL   : double;
    function GetTradeFee : double;
    function GetTradeAmtDiff( sType : TSPType ) : double;
    function GetTradeAmt(bSum:boolean) : double;
    function GetActiveOrderQty(aType: TAutoOrderType; sType: TSPType; iSide : integer): double;
    function GetPriceInfo(aSymbol : TSymbol; bEntry: boolean): string;

    function CheckMajorAvailableAmt(dOrderPrice, dOrderQty: double): boolean;
    function CheckMajorAvailableBalance(dOrderQty: double): boolean;
  public
    FTimer2 : TTimer;
    // 회전율을 구하기 위해 매도/매수 구별 없이 거래량 누적
    AccVolume: array [TSPType] of double;
    // 주거래소로의 주문 거부 수량 ( 반드시 체결 시켜야 하는 수량 )
    MustOrdQty : array [TPositionType] of double;

    constructor Create( aColl : TCollection ); override;
    Destructor  Destroy; override;

    function Start : boolean;
    procedure Stop ;
    procedure Init( aItem : TSPParam ); overload;
    procedure Init( aSymbols : TSPSymbolArray;  aMsgQuotes : TMergedQuoteArray;
      aItem : TSPParam ); overload;
    procedure OnParam( aItem : TSPParam );

    procedure OnMajorQuote( aSymbol : TSymbol );
    procedure OnSubQuote( aSymbol : TSymbol );
    procedure DoAllOrderCancel;

    //function GetVolume( sType : TSPType ) : double;
    function GetVolume( bBuy : boolean ) : double;
    function GetAccVolumeGap : double;
    function CheckAccVolumeDiff(aType : TAutoOrderType) : boolean;

    property Symbols : TSPSymbolArray read FSymbols;
    property Accounts: TSPAccountArray read FAccounts;
    property Positions: TSPPositionArray read FPositions;
    property MgQuotes: TMergedQuoteArray read FMgQuotes;
    property Assets : TSPAssetArray read FAssets;

    // 주문관리
    property OrderUnit  : TSPOrdUnitArray read FOrderUnit;
    property ModifyList : TList read FModifyList;

    //
    property Run : boolean read FRun;
    property Param : TSPParam read FParam write SetParam;
    property SPValue : TSPArray read FSPValue;
    property Name : string read FName write FName;
    property No   : integer read FNo write FNo;

    // 모니터링용
    property BasePrice : TSPArray read FBasePrice;
    property TargetPrice : TSPArray read FTargetPrice;
    property OrderPrice: TSPArray read FOrderPrice;
    property CompPrice : TSPArray read FCompPrice;

    property TradeRate : double read GetTradeRate;
    property TradePL   : double read GetTradePL;
    property BasePosQty: double read FBasePosQty;

    property OnSPNotify : TTextNotifyEvent read FOnSPNotify write FOnSPNotify;
    property OnSPNotifyEx : TTextNotifyEvent read FOnSPNotify2 write FOnSPNotify2;
    property OnQuoteNofity : TQuoteNotifiy read FOnQuoteNotify write FOnQuoteNotify;
    // flag
    property Ready : TReadyArray read FReady write FReady;


  end;

  TSPOrders = class( TCollection )
  private
    FSelect: TSPOrder;
    function GetSPOrders(i: Integer): TSPOrder;
  public
    LastGetTime : array [TSPType] of TDateTime;

    constructor Create;
    destructor Destroy; override;

    function New( stCode : string ) : TSPOrder; overload;
    function New( idx : integer ) : TSPOrder; overload;
    function Find( stCode : string ) : TSPOrder;

    function GetBalance(sType : TSPType) : double;
    function GetTotTradeRate : double;
    function GetTotTradePL   : double;
    function GetTotTradePLForComp  : double;
    function GetTotFee       : double;
    function GetAccVolumeGap : double;
    function GetTotTradeAmt(bSum:boolean) : double;
    function GetTotTradeAmtDiff( sType : TSPType ) : double;

    procedure GetTotalValue( var dValue : array of double ) ;

    function IsWaiting(sType : TSPType): boolean;
    procedure SetTime(sType : TSPType);

    //procedure init( aParam : TSPParam; aNotify : TTextNotifyEvent );
    procedure OnTrade( aData : TObject; iEvent : integer );

    property SPOrders[i : Integer] : TSPOrder read GetSPOrders; default;
    property Select : TSPOrder read FSelect write FSelect;
    //property LastGetTime : TDateTime read FLastGetTime;
  end;

implementation

uses
  GApp, GLibs
  , UApiConsts
  , System.Math, System.Types
  , UConsts


  ;

{ TSPOrder }

procedure TSPOrder.CalcSPValue;
begin

  if CheckZero( FSymbols[spMajor].Bids[0].Price ) then
    FSPValue[aoEntry] := 0
  else
    FSPValue[aoEntry] := ( FSymbols[spSub].Bids[0].Price - FSymbols[spMajor].Bids[0].Price )
      / FSymbols[spMajor].Bids[0].Price;

  if CheckZero( FSymbols[spMajor].Asks[0].Price ) then
    FSPValue[aoExit] := 0
  else
    FSPValue[aoExit] := ( FSymbols[spSub].Asks[0].Price - FSymbols[spMajor].Asks[0].Price )
      / FSymbols[spMajor].Asks[0].Price;
end;

constructor TSPOrder.Create(aColl: TCollection);
var
  I: TSPType;
  j: TAutoOrderType;
begin
  inherited Create( aColl );

  FRun := false;

  for I := spMajor to High(TSPType) do
  begin
    FSymbols[i]   := nil;
    FAccounts[i]  := nil;
    FAssets[i]    := nil;

    FMgQuotes[i]  := TMergedQuote.Create;
    FPositions[i] := nil;//TSPPosition.Create( nil );

  end;

  for j := aoEntry to High(TAutoOrderType) do
  begin
    FTimer[j] := TTimer.Create(nil);
    FTimer[j].OnTimer  := OnDelayTimer;
    FTimer[j].Enabled  := false;
    FTimer[j].Tag      := integer(j);

    FOrderUnit[j] := TSpOrderUnit.Create;

    FModifyOrderList[j] := TModifyOrderList.Create(self);

    FLastOrdQty[j]      := 0.0;
  end;

  FTimer2 := TTimer.Create(nil);
  FTimer2.OnTimer  := OnMustOrderTimer;
  FTimer2.Enabled  := false;
  FTimer2.Interval := 1000;

  MustOrdQty[ptLong]  := 0;
  MustOrdQty[ptShort] := 0;

end;

destructor TSPOrder.Destroy;
var
   I: TSPType;
   j: TAutoOrderType;
begin

  for j := aoEntry to High(TAutoOrderType) do
  begin
    FTimer[j].Free;
    FOrderUnit[j].Free;
    FModifyOrderList[j].Free;
  end;

  for I := spMajor to High(TSPType) do
  begin
//    FMgQuotes[i].Free;
    FPositions[i].Free;
  end;

  inherited;
end;

procedure TSPOrder.DoAllOrderCancel;
begin
  FOrderUnit[aoEntry].DoCancelOrder(QS,'');
  FOrderUnit[aoExit].DoCancelOrder(QS,'');
end;

procedure TSPOrder.DoLog(stLog: string; bShow : boolean );
begin
  stLog := FName + ' : ' + stLog;
  App.Log( llInfo, FParam.Code + '_SP', ' %s', [ stLog]  );
  if bShow and  Assigned( FOnSPNotify) then
    FOnSPNotify( Self, stLog );
end;

procedure TSPOrder.DoMustOrder(ptType: TPositionType);
var
  dAmt, dQty, dPrice : double;
  aOrder  : TOrder;
  aType   : TAutoOrderType;
begin

  if IsZero(MustOrdQty[ptType]) then Exit;

  if ptType = ptLong then begin
    if FParam.UseHogaMerge then
      dPrice := FMgQuotes[spMajor].Asks[0].Price
    else
      dPrice := FSymbols[spMajor].Asks[0].Price;
    aType := aoExit;
    // 주거래소 매수 주문 가능 금액 구하기...
    if not CheckMajorAvailableAmt( dPrice, MustOrdQty[ptType]) then Exit;

  end else
  begin
    if FParam.UseHogaMerge then
      dPrice := FMgQuotes[spMajor].Bids[0].Price
    else
      dPrice := FSymbols[spMajor].Bids[0].Price;
    aType := aoEntry;

    if not CheckMajorAvailableBalance(MustOrdQty[ptShort]) then Exit;
  end;

  if dPrice * MustOrdQty[ptType] < MIN_ORD_AMT then Exit;

  aOrder  := DoOrder( FAccounts[spMajor], FSymbols[spMajor],  ifThen(ptType=ptLong,1,-1),
      dPrice, MustOrdQty[ptType],  aType = aoEntry );

  if aOrder <> nil then begin
    MustOrdQty[ptType] := 0;
    DoLog( '체결차 발생으로 인한 주문',true);
    FOrderUnit[aType].OrderList[spMajor].Add( aOrder );
    FModifyOrderList[aType].New( FParam, OnModifyOrderLog ).SetOrder( aOrder, true )  ;
  end ;

end;

function TSPOrder.GetPriceInfo( aSymbol : TSymbol; bEntry : boolean ) : string;
var
  aType : TAutoOrderType;
begin

  if bEntry then
    aType := aoEntry
  else
    aType := aoExit;

  Result := Format('B:%s, C:%s, M:%s', [
        aSymbol.PriceToStr(FBasePrice[aType]),
        FmtString(4, FTargetPrice[aType]),
        aSymbol.PriceToStr(FCompPrice[aType])
         ] );
end;

function TSPOrder.DoOrder( aAcnt : TAccount; aSymbol : TSymbol; iSide: integer; dPrice: double;
  dOrderVol : double;  bEntry: boolean): TOrder;
  var
    sType : TSPType;
begin

  Result := nil;

  if (aAcnt=nil) or (aSymbol=nil)  then Exit;

  if IsZero(dPrice) or IsZero(dOrderVol) then
  begin
    DoLog( Format('%s wrong parameter %f, %f', [ ifThenStr( bEntry,'진입','청산')
      , dPrice, dOrderVol ]));
    Exit;
  end;

  if not App.Engine.TradeCore.CheckLimitOrder( aAcnt, aSymbol, iSide, dPrice, dOrderVol, ORD_AUTO) then
  begin
    DoLog( '주문 제한에 걸림', true );
    Exit;
  end;

  Result := App.Engine.TradeCore.Orders[ aSymbol.Spec.ExchangeType ].NewOrder(
    aAcnt, aSymbol, iSide, dOrderVol, pcLimit, dPrice, tmGTC
  );

  if Result <> nil then
  begin
    Result.StgType  := stSPOrder;
    Result.Entry    := bEntry;
    Result.GroupNo  := FName;
    App.Engine.TradeBroker.Send(Result);

    if aSymbol.Spec.ExchangeType = FParam.SubEx then
      sType := spSub
    else
      sType := spMajor;

    TSPOrders( Collection ).SetTime(sType);

    DoLog( Format('%s 주문 -> %s, (%s) ', [ ifThenStr( bEntry,'진입','청산'),
      Result.Represent  , GetPriceInfo( aSymbol, bEntry )
       ]  ), true );

  end else
  begin
    DoLog( Format('%s 주문 생성 실패 (%s %s)', [ ifThenStr( bEntry, '진입','청산'),
      ExKindToStr( aSymbol.Spec.ExchangeType ),
      ifThenStr( iSide = 1, '매수','매도') ]), true ) ;
    Exit;
  end;
end;

procedure TSPOrder.Init( aItem : TSPParam );
var
  I: TSPType;
begin

  FSymbols[spMajor] := App.Engine.SymbolCore.BaseSymbols.FindSymbol(aItem.Code, aItem.MajorEx);
  FSymbols[spSub]   := App.Engine.SymbolCore.BaseSymbols.FindSymbol(aItem.Code, aItem.SubEx  );

  FAccounts[spMajor]  := App.Engine.TradeCore.FindAccount( aItem.MajorEx );
  FAccounts[spSub]    := App.Engine.TradeCore.FindAccount( aItem.SubEx );

  FMgQuotes[spMajor].SetSymbol( FSymbols[spMajor], aItem.HogaMergeUnit );
  FMgQuotes[spSub].SetSymbol( FSymbols[spSub], aItem.HogaMergeUnit );

  FBasePosQty := 0;
  for I := spMajor to High(TSPType) do
  begin
    if FPositions[i] <> nil then
      FPositions[i].Free;
    FPositions[i] := TSPPosition.Create(nil);
    FPositions[i].Account := FAccounts[i];
    FPositions[i].Symbol  := FSymbols[i];

    FOrderUnit[TAutoOrderType(i)].Init( OnOrderUnitLog, TAutoOrderType(i) = aoEntry );
    FReady[TAutoOrderType(i)] := false;

    if FAccounts[i] <> nil then begin
      FAssets[i]  := FAccounts[i].Assets.Find(aItem.Code);
      if FAssets[i] = nil then
        FAssets[i]  := FAccounts[i].Assets.New(aItem.Code, FAccounts[i]);
    end;

    AccVolume[i] := 0;

    FBasePosQty := FBasePosQty + FAssets[i].Balance;
  end;
  FParam  := aItem;
end;

procedure TSPOrder.Init(aSymbols: TSPSymbolArray; aMsgQuotes: TMergedQuoteArray;
  aItem: TSPParam);
var
  I: TSPType;
begin
  for I := spMajor to High(TSPType) do
  begin
    FSymbols[I] := aSymbols[I];
    FMgQuotes[i]:= aMsgQuotes[I];
  end;

  FAccounts[spMajor]  := App.Engine.TradeCore.FindAccount( aItem.MajorEx );
  FAccounts[spSub]    := App.Engine.TradeCore.FindAccount( aItem.SubEx );

  FBasePosQty := 0;

  for I := spMajor to High(TSPType) do
  begin
    if FPositions[i] <> nil then
      FPositions[i].Free;
    FPositions[i] := TSPPosition.Create(nil);
    FPositions[i].Account := FAccounts[i];
    FPositions[i].Symbol  := FSymbols[i];

    FOrderUnit[TAutoOrderType(i)].Init( OnOrderUnitLog, TAutoOrderType(i) = aoEntry );
    FReady[TAutoOrderType(i)] := false;

    if FAccounts[i] <> nil then begin
      FAssets[i]  := FAccounts[i].Assets.Find(aItem.Code);
      if FAssets[i] = nil then
        FAssets[i]  := FAccounts[i].Assets.New(aItem.Code, FAccounts[i]);
    end;

    AccVolume[i] := 0;

    FBasePosQty := FBasePosQty + FAssets[i].Balance;
  end;

  FParam  := aItem;

  MustOrdQty[ptLong]  := 0;
  MustOrdQty[ptShort] := 0;

end;

function TSPOrder.IsRun: boolean;
begin
  Result := false;

  if not FRun then Exit;
  if ( FSymbols[spMajor] = nil ) or ( FSymbols[spSub] = nil ) then Exit;
  if ( FAccounts[spMajor] = nil ) or ( FAccounts[spSub] = nil ) then Exit;

  Result := true;
end;

function TSPOrder.GetMajorBasePrice( bEntry : boolean; bCheckVol : boolean ) : TMarketDepth;
var
  aDepths : TMarketDepths;
begin

  if FParam.UseHogaMerge then begin
    if bEntry then
      aDepths := FMgQuotes[spMajor].Bids
    else
      aDepths := FMgQuotes[spMajor].Asks;
  end else
  begin
    if bEntry then
      aDepths := FSymbols[spMajor].Bids
    else
      aDepths := FSymbols[spMajor].Asks;
  end;

  if bCheckVol then
    Result := aDepths.GetBiggerDepth(FParam.MinVolume)
  else
    Result := aDepths.Depths[0];
end;

function TSPOrder.GetOrderQty( aType : TAutoOrderType): double;
var
  dQty2, dQty : double;
begin
  if FPositions[spSub] = nil then Exit (FParam.EntryQty);

  if aType = aoEntry then
    dQty2 := FParam.LimitEntryPos - ( FPositions[spSub].BuyVol - FPositions[spSub].SellVol )
  else
    dQty2 := FParam.LimitLiquidPos - ( FPositions[spSub].SellVol - FPositions[spSub].BuyVol );

  if FParam.UseVolumeRate then
  begin
    dQty := SimpleRoundTo( (FBaseVolume[aType] * ( FParam.VolumeRate / 100 )), -4);
    if dQty2 > dQty then
      Result := min( FParam.EntryQty, dQty )
    else
      Result := min( FParam.EntryQty, dQty2 );
  end else
  begin
    Result := min(FParam.EntryQty, dQty2);
  end;

end;

function TSPOrder.GetSubCompPrice(bEntry: boolean): double;
var
  dTmp : double;
  res : TValueRelationship;
begin
  case FParam.BasePrice of
    0 : if bEntry then
          Result := TicksFromPrice( FSymbols[spSub], FSymbols[spSub].Asks[0].Price, -FParam.BaseTick )
        else
          Result := TicksFromPrice( FSymbols[spSub], FSymbols[spSub].Bids[0].Price, FParam.BaseTick );
    1 : if bEntry then
          Result := TicksFromPrice( FSymbols[spSub], FSymbols[spSub].Last, -FParam.BaseTick )
        else
          Result := TicksFromPrice( FSymbols[spSub], FSymbols[spSub].Last, FParam.BaseTick );
    2 : if bEntry then
        begin

          dTmp := TicksFromPrice( FSymbols[spSub], FSymbols[spSub].Bids[0].Price, FParam.BaseTick );
          // 매도호가가 더 커야 한다.
          res := CompareValue( dTmp, FSymbols[spSub].ASks[0].Price );

          if res >= 0 then
            Result := TicksFromPrice( FSymbols[spSub], FSymbols[spSub].ASks[0].Price, -1 )
          else
            Result := dTmp;

//          DoLog( Format('-- 진입 %s, %s->%s, %s (%d,%d) --', [ FSymbols[spSub].PriceToStr(FSymbols[spSub].ASks[0].Price), FSymbols[spSub].PriceToStr(dTmp),
//            FSymbols[spSub].PriceToStr(Result), FSymbols[spSub].PriceToStr(FSymbols[spSub].Bids[0].Price), res, FParam.BaseTick ] ));

        end else
        begin

          dTmp := TicksFromPrice( FSymbols[spSub], FSymbols[spSub].ASks[0].Price, -FParam.BaseTick );
          // 매수호가가 더 커야 한다.
          res := CompareValue( FSymbols[spSub].Bids[0].Price, dTmp );

          if res >= 0 then
            Result := TicksFromPrice( FSymbols[spSub], FSymbols[spSub].Bids[0].Price, 1 )
          else
            Result := dTmp;

//          DoLog( Format('-- 청산 %s, %s->%s, %s (%d,%d) --', [ FSymbols[spSub].PriceToStr(FSymbols[spSub].ASks[0].Price), FSymbols[spSub].PriceToStr(dTmp),
//            FSymbols[spSub].PriceToStr(Result), FSymbols[spSub].PriceToStr(FSymbols[spSub].Bids[0].Price), res, FParam.BaseTick ] ));
        end;
//    2 : if bEntry then
//          Result := FSymbols[spSub].Bids[0].Price //TicksFromPrice( FSymbols[spSub], FSymbols[spSub].Bids[0].Price, FParam.BaseTick )
//        else
//          Result := FSymbols[spSub].ASks[0].Price; //TicksFromPrice( FSymbols[spSub], FSymbols[spSub].ASks[0].Price, -FParam.BaseTick );
  end;
end;

function TSPOrder.GetSubEntryPrice( dTargetPrice, dBasePrice : double; var dEntryPrice : double ) : boolean;
var
  i : integer;
  dPrice : double;
  res : TValueRelationship;
begin
  Result := false;

  res := CompareValue( dTargetPrice, dBasePrice );

  if res >= 0  then begin
    dEntryPrice := dBasePrice;
    Result      := true;
  end
  else
    // 호가 제한 적용 ( 2호가만 )
    for I := 0 to 2-1 do
    begin
      dPrice  := TicksFromPrice( FSymbols[spSub], dBasePrice, -i );
      res := CompareValue( dTargetPrice, dPrice );
      if res >= 0 then
      begin
        dEntryPrice := dPrice;
        Result      := true;
        break;
      end;
    end;

//    i := 1;
//    while (true) do
//    begin
//      dPrice  := TicksFromPrice( FSymbols[spSub], dBasePrice, -i );
//      inc(i);
//      res := CompareValue( dTargetPrice, dPrice );
//      //if dTargetPrice - DOUBLE_EPSILON > dPrice then
//      if res >= 0 then
//      begin
//        dEntryPrice := dPrice;
//        Result      := true;
//        break;
//      end;
//    end;


//
//  if dTargetPrice - DOUBLE_EPSILON > dBasePrice then begin
//    dEntryPrice  := dBasePrice;
//    Result       := true;
//  end else
//  begin
//    i := 1;
//      while (true) do
//      begin
//        dPrice  := TicksFromPrice( FSymbols[spSub], dBasePrice, -i );
//        inc(i);
//        if dTargetPrice - DOUBLE_EPSILON > dPrice then
//        begin
//          dEntryPrice := dPrice;
//          Result      := true;
//          break;
//        end;
//      end;
//  end;
end;
                                                 // compPrice
function TSPOrder.GetSubLiquidPrice( dTargetPrice, dBasePrice : double; var dLiqPrice : double ) : boolean;
var
  i : integer;
  dPrice : double;
  res : TValueRelationship;
begin
  Result := false;

  res := CompareValue( dTargetPrice, dBasePrice );

  if res <= 0 then
  begin
    dLiqPrice := dBasePrice;
    Result    := true;
  end else
    // 호가 제한 적용
    for I := 0 to 1 do //FParam.BaseTick-1 do
    begin
      dPrice  := TicksFromPrice( FSymbols[spSub], dBasePrice, i );
      res := CompareValue( dTargetPrice, dPrice );
      if res <= 0 then
      begin
        dLiqPrice := dPrice;
        Result    := true;
        break;
      end;
    end;

//    i := 1;
//
//    while (true) do
//    begin
//      dPrice  := TicksFromPrice( FSymbols[spSub], dBasePrice, i );
//      inc(i);
//      // targetPrice 보다 큰 호가..
//      res := CompareValue( dTargetPrice, dPrice );
//      //if dTargetPrice < dPrice - DOUBLE_EPSILON then
//      if res <= 0 then
//      begin
//        dLiqPrice := dPrice;
//        Result    := true;
//        break;
//      end;
//    end;


//  if dTargetPrice < dBasePrice - DOUBLE_EPSILON then begin
//    dLiqPrice  := dBasePrice;
//    Result       := true;
//  end else
//  begin
//    i := 1;
//    while (true) do
//    begin
//      dPrice  := TicksFromPrice( FSymbols[spSub], dBasePrice, i );
//      inc(i);
//      // targetPrice 보다 큰 호가..
//      //if dTargetPrice < dPrice - DOUBLE_EPSILON then
//      if dTargetPrice - DOUBLE_EPSILON < dPrice  then
//      begin
//        dLiqPrice := dPrice;
//        Result    := true;
//        break;
//      end;
//    end;
//  end;
end;

function TSPOrder.GetTradeAmt(bSum: boolean): double;
var
  d : array [TSPType] of double;
  i : TSPType;
begin
  Result := 0;
  if (FPositions[spMajor] = nil) or (FPositions[spSub]=nil) then Exit;

  for I := spMajor to High(TSPType) do
    d[I]:= FPositions[I].BuyAmt + FPositions[I].SellAmt;

  if bSum then
    Result := d[spSub] + d[spMajor]
  else
    Result := d[spMajor] - d[spSub];
end;

function TSPOrder.GetTradeAmtDiff(sType: TSPType): double;
begin
  Result := 0;
  if FPositions[sType] = nil then Exit;

  Result := FPositions[sType].BuyAmt - FPositions[sType].SellAmt;
end;

function TSPOrder.GetTradeFee: double;
begin
  Result := 0;
  if (FPositions[spMajor] = nil) or (FPositions[spSub]=nil) then Exit;

  Result := FPositions[spMajor].Fee + FPositions[spSub].Fee ;
end;

function TSPOrder.GetTradePL: double;
//var
  //dTmp : double;
begin
  Result := 0;
  if (FPositions[spMajor] = nil) or (FPositions[spSub]=nil) then Exit;

  // 아래 수식에서 다시 원복...
  Result := (FPositions[spMajor].SellAmt + FPositions[spSub].SellAmt) -
              (FPositions[spMajor].BuyAmt + FPositions[spSub].BuyAmt) -
                (FPositions[spMajor].Fee + FPositions[spSub].Fee) ;

//  Result :=
//  ((FPositions[spMajor].BuyVol * FPositions[spMajor].Symbol.Last) - FPositions[spMajor].BuyAmt)+
//  (FPositions[spMajor].SellAmt - (FPositions[spMajor].SellVol * FPositions[spMajor].Symbol.Last))+
//
//  ((FPositions[spSub].BuyVol * FPositions[spSub].Symbol.Last) - FPositions[spSub].BuyAmt)+
//  (FPositions[spSub].SellAmt - (FPositions[spSub].SellVol * FPositions[spSub].Symbol.Last))
//
//  -(FPositions[spMajor].Fee + FPositions[spSub].Fee)
  ;
  //Result := Round(dTmp / 10000);
end;

function TSPOrder.GetLastPL: double;
begin
  Result := 0;
  if (FPositions[spMajor] = nil) or (FPositions[spSub]=nil) then Exit;

  // 아래 수식에서 다시 원복...
  Result := FPositions[spMajor].LastPL + FPositions[spSub].LastPL;

end;

function TSPOrder.GetTradeRate: double;
//var
//  dVol : double;
begin

//  if (FPositions[spMajor] = nil) or (FPositions[spSub]=nil) then Exit;
//
//  dVol  := abs(FPositions[spMajor].Volume) + abs(FPositions[spSub].Volume);

  if CheckZero(FBasePosQty) then
    Result := 0
  else
    Result :=  AccVolume[spMajor] / FBasePosQty * 100;
end;


function TSPOrder.GetVolume( bBuy : boolean ) : double;
begin
  if FPositions[spSub] <> nil then begin
    if bBuy then
      Result := FPositions[spSub].BuyVol
    else
      Result := FPositions[spSub].SellVol;
  end
  else
    Result := 0;
end;

function TSPOrder.SetPrice( bEntry : boolean ) : boolean;
var
  aDepth : TMarketDepth;
  dOrdPrice, dCompPrice, dTargetPrice : double;
  aType : TAutoOrderType;
  iDelay : integer;
  stLog  : string;
begin

  Result := false;
  aDepth := nil;

  try

    aDepth := GetMajorBasePrice(bEntry, true);
    if aDepth = nil then begin
      stLog := ifThenStr( bEntry, '10', '-10');
      Exit;
    end;

    dOrdPrice := 0;

    if bEntry then
    begin
      aType := aoEntry;
      dTargetPrice:= aDepth.Price * (1 + (FParam.EntryRate)/100);
      dCompPrice  := GetSubCompPrice(bEntry);

      if not GetSubEntryPrice( dTargetPrice, dCompPrice, dOrdPrice ) then
      begin
      //DoLog( Format(' not Found lesser than %s ', [ FSymbols[spSub].PriceToStr( dTargetPrice) ]));
        stLog := '20';
        Exit;
      end;

    end else
    begin
      aType := aoExit;
      dTargetPrice:= aDepth.Price * (1 + (FParam.LiquidRate)/100);
      dCompPrice  := GetSubCompPrice(bEntry);

      if not GetSubLiquidPrice( dTargetPrice, dCompPrice, dOrdPrice ) then
      begin
      //DoLog( Format(' not Found lesser than %s ', [ FSymbols[spSub].PriceToStr( dTargetPrice) ]));
        stLog := '-20';
        Exit;
      end;
    end;

    FTargetPrice[aType] := dTargetPrice;
    FBasePrice[aType] := aDepth.Price;
    FBaseVolume[aType]:= aDepth.Volume;
    FOrderPrice[aType]:= dOrdPrice;
    FCompPrice[aType] := dCompPrice;

    REsult := true;
  finally

    if (not Result) and Assigned( FOnSPNotify2 ) then
      FOnSPNotify2( Self, stLog );
  end;


end;

function TSPOrder.SetPrice2(bEntry: boolean): boolean;
var
  aDepth : TMarketDepth;
  aType : TAutoOrderType;
begin

  Result := false;

  aDepth := GetMajorBasePrice(bEntry, false);
  if aDepth = nil then Exit;

  aType := TAutoOrderType( IfThen(bEntry, integer(aoEntry), integer(aoExit)) );

  FBasePrice[aType] := aDepth.Price;
  FBaseVolume[aType]:= aDepth.Volume;

  REsult := true;
end;

procedure TSPOrder.OnDelayTimer(Sender: TObject);
var
  aType : TAutoOrderType;
begin
  try
    aType := TAutoOrderType( (Sender as TComponent).Tag);
    FReady[aType] := false;
//    DoLog( Format('--- %s Flag false (%d) ---' , [ ifThenStr( aType=aoEntry,'진입','청산'),
//       FOrderUnit[aType].OrderList[spSub].Count] ) , true );

    DoLog( Format('%s 섭주문 재개 (%d / %d) timer' , [ ifthenStr(aType=aoEntry,'진입','청산'),
       FOrderUnit[aType].OrderList[spSub].Count , FOrderUnit[aType].OrderList[spMajor].Count ] ) , true );
  finally
    (Sender as TTimer).Enabled := false;
  end;
end;

procedure TSPOrder.OnPave( bEntry : boolean );
var
  dTradeRate : double;
begin
  if not SetPrice(bEntry) then Exit;
{
  if GetTradePL < FParam.LimitPL then
  begin
    FRun := false;
    DoLog('--- Stop 손익 한도---', true);
    Exit;
  end;

  dTradeRate := GetTradeRate;
  if dTradeRate > FParam.LimitTradeAmt then
  begin
    FRun := false;
    DoLog( Format('--- Stop 회전율 한도 %.0f%% ---' , [dTradeRate]) , true);
    Exit;
  end;
}
  if bEntry then
    OnEntry
  else
    OnLiquid;
end;

procedure TSPOrder.OnEntry;
var
//  i : integer;
//  dAvailable, dOrderAmt,
  dOrderPrice, dOrderQty : double;
//  aAsset : TAsset;
  aOrder : TOrder;
begin

  dOrderQty   := GetOrderQty( aoEntry );
  if not CheckEntryLimit(dOrderQty) then
  begin
//    FRun := false;
//    FParam.UseEntry := false;
//    DoLog( '--- 진입 Stop ---' , true );
    Exit;
  end;

  // 연속으로 주문 내면 거부 가능성이 높음
  if not TSPOrders( Collection ).IsWaiting(spSub) then Exit;

  dOrderPrice := FOrderPrice[aoEntry]; //TicksFromPrice( FSymbols[spSub], FOrderPrice[aoEntry], -1*i*FParam.EntryTick );

  aOrder := DoOrder( FAccounts[spSub], FSymbols[spSub], 1, dOrderPrice, dOrderQty, true );

  if aOrder <> nil then
    FOrderUnit[aoEntry].OrderList[spSub].Add( aOrder );

  if FOrderUnit[aoEntry].OrderList[spSub].Count > 0 then
  begin
    FOrderUnit[aoEntry].BasePrice     := FBasePrice[aoEntry] ;
    FOrderUnit[aoEntry].BasePriceStr  := FSymbols[spMajor].PriceToStr(FBasePrice[aoEntry]);

    FOrderUnit[aoEntry].OrderPrice    := FOrderPrice[aoEntry];
    FOrderUnit[aoEntry].OrderPriceStr := FSymbols[spSub].PriceToStr(FOrderPrice[aoEntry]);

    FReady[aoEntry] := true;

    if Assigned( FOnSPNotify2 ) then
      FOnSPNotify2( Self, '100' );
//    DoLog( Format('--- 진입 Flag True (%d) ---' , [FOrderUnit[aoEntry].OrderList[spSub].Count] ) , true );
  end;
end;

procedure TSPOrder.OnLiquid;
var
//  i : integer;
//  dAvailable, dOrdQty ,
  dOrderPrice, dOrderQty : double;
//  aAsset : TAsset;
  aOrder : TOrder;
begin

  dOrderQty   := GetOrderQty( aoExit );
  if not CheckLiquidLimit(dOrderQty) then
  begin
//    FParam.UseLiquid := false;
//    DoLog( '-- 청산 Stop ---' , true );
    Exit;
  end;

  if not TSPOrders( Collection ).IsWaiting(spSub) then Exit;

  dOrderPrice :=  FOrderPrice[aoExit]; //TicksFromPrice( FSymbols[spSub], FOrderPrice[aoExit], i*FParam.LiquidTick );

  aOrder := DoOrder( FAccounts[spSub], FSymbols[spSub], -1, dOrderPrice, dOrderQty, false );

  if aOrder <> nil then
    FOrderUnit[aoExit].OrderList[spSub].Add( aOrder );

  if FOrderUnit[aoExit].OrderList[spSub].Count > 0 then
  begin
    FOrderUnit[aoExit].BasePrice     := FBasePrice[aoExit] ;
    FOrderUnit[aoExit].BasePriceStr  := FSymbols[spMajor].PriceToStr(FBasePrice[aoExit]);

    FOrderUnit[aoExit].OrderPrice    := FOrderPrice[aoExit];
    FOrderUnit[aoExit].OrderPriceStr := FSymbols[spSub].PriceToStr(FOrderPrice[aoExit]);

    FReady[aoExit] := true;

    if Assigned( FOnSPNotify2 ) then
      FOnSPNotify2( Self, '-100' );
//    DoLog( Format('--- 청산 Flag True (%d) ---' , [FOrderUnit[aoExit].OrderList[spSub].Count] ) , true );
  end;

end;


procedure TSPOrder.OnWatchEntryOrder;
var
  sReason: string;
  res  : TValueRelationship;
begin
  if not SetPrice2(true) then Exit;

//  stPrice := FSymbols[spMajor].PriceToStr( FBasePrice[aoEntry] );
//  if stPrice = FOrderUnit[aoEntry].BasePriceStr then Exit;
//
//  stPrice := FSymbols[spSub].PriceToStr( FOrderPrice[aoEntry] );
//  if stPrice = FOrderUnit[aoEntry].OrderPriceStr then Exit;

  res := CompareValue( FOrderUnit[aoEntry].BasePrice, FBasePrice[aoEntry] );

  if res > 0 then begin
    // 기준가 하락
    sReason := Format('%s -> %s', [ FOrderUnit[aoEntry].BasePriceStr, FSymbols[spMajor].PriceToStr(FBasePrice[aoEntry]) ] );
    FOrderUnit[aoEntry].DoCancelOrder(BD, sReason);
  end else
  if res < 0 then begin
    // 기준가 상승
    sReason := Format('%s -> %s', [ FOrderUnit[aoEntry].BasePriceStr, FSymbols[spMajor].PriceToStr(FBasePrice[aoEntry]) ] );
    FOrderUnit[aoEntry].DoCancelOrder(BU, sReason);
  end else
  if res = 0 then begin
    // 잔량 줄어들때
    if FBaseVolume[aoEntry] < FParam.MinVolume then begin
      sReason := Format('%.0n > %.0n', [  FParam.MinVolume, FBaseVolume[aoEntry] ] );
      FOrderUnit[aoEntry].DoCancelOrder(BVD, sReason);
    end;
  end;

//  if FOrderUnit[aoEntry].BasePrice - EPSILON > FBasePrice[aoEntry] then begin
//    // 기준가 하락.
//    //DoLog( Format('Entry Orders 기준가하락에 의한 취소 %s --> %s', [ FOrderUnit[aoEntry].BasePriceStr, stPrice ]), true);
//    FOrderUnit[aoEntry].DoCancelOrder(BD);
//  end
//  else if FBasePrice[aoEntry] - EPSILON > FOrderUnit[aoEntry].BasePrice then begin
//    // 기준가 상승.
//    //DoLog( Format('Entry Orders 기준가상승에 의한 취소 %s --> %s', [ FOrderUnit[aoEntry].BasePriceStr, stPrice ]), true);
//    FOrderUnit[aoEntry].DoCancelOrder(BU);
//  end
//  else begin
//
//  end;
end;

procedure TSPOrder.OnWatchLiquidOrder;
var
  sReason: string;
  res  : TValueRelationship;
begin
  if not SetPrice2(false) then Exit;

//  stPrice := FSymbols[spMajor].PriceToStr( FBasePrice[aoExit] );
//  if stPrice = FOrderUnit[aoExit].BasePriceStr then Exit;
//  stPrice := FSymbols[spSub].PriceToStr( FOrderPrice[aoExit] );
//  if stPrice = FOrderUnit[aoExit].OrderPriceStr then Exit;

  res := CompareValue( FOrderUnit[aoExit].BasePrice, FBasePrice[aoExit] );

  if res > 0 then begin
    // 기준가 하락
    sReason := Format('%s -> %s', [ FOrderUnit[aoExit].BasePriceStr, FSymbols[spMajor].PriceToStr(FBasePrice[aoExit]) ] );
    FOrderUnit[aoExit].DoCancelOrder(BD, sReason);
  end else
  if res < 0 then begin
    // 기준가 상승
    sReason := Format('%s -> %s', [ FOrderUnit[aoExit].BasePriceStr, FSymbols[spMajor].PriceToStr(FBasePrice[aoExit]) ] );
    FOrderUnit[aoExit].DoCancelOrder(BU, sReason);
  end else
  if res = 0 then begin
    // 잔량 줄어들때
    if FBaseVolume[aoExit] < FParam.MinVolume then begin
      sReason := Format('%.0n > %.0n', [ FParam.MinVolume, FBaseVolume[aoExit] ] );
      FOrderUnit[aoExit].DoCancelOrder(BVD, sReason);
    end;
  end;


//  if FOrderUnit[aoExit].BasePrice - EPSILON > FBasePrice[aoExit] then begin
//    // 기준가 하락.
//    //DoLog( Format('Exit Orders 기준가하락에 의한 취소 %s --> %s', [ FOrderUnit[aoExit].BasePriceStr, stPrice ]), true);
//    FOrderUnit[aoExit].DoCancelOrder(BD);
//  end
//  else if FBasePrice[aoExit] - EPSILON > FOrderUnit[aoExit].BasePrice then begin
//    // 기준가 상승.
//    //DoLog( Format('Exit Orders 기준가상승에 의한 취소 %s --> %s', [ FOrderUnit[aoExit].BasePriceStr, stPrice ]), true);
//    FOrderUnit[aoExit].DoCancelOrder(BU);
//  end else
//    Exit;
end;

procedure TSPOrder.OnWathcMajorOrder;
var
  I: TAutoOrderType;
begin

  for I := aoEntry to High(TAutoOrderType) do
    if TSPOrders( Collection ).IsWaiting(spMajor) then
      FModifyOrderList[i].WatchOrder(FParam, FMgQuotes[spMajor] );
end;

procedure TSPOrder.OnMajorQuote(aSymbol: TSymbol);
begin

  if not IsRun then Exit;

  OnWathcMajorOrder;

  if FParam.UseEntry then begin
    if not FReady[aoEntry] then
      OnPave(true)
    else
      OnWatchEntryOrder;
  end;

  // 청산
  if FParam.UseLiquid then begin
    if not FReady[aoExit] then
      OnPave(false)
    else
      OnWatchLiquidOrder;
  end;

  if Assigned(FOnQuoteNotify) then
    FOnQuoteNotify(Self, spMajor);

end;

procedure TSPOrder.OnSubQuote(aSymbol: TSymbol);
begin
  if not IsRun then Exit;
  // 진입
//  if not FReady[aoEntry] then
//    OnEntry
//  else
//    OnWatchEntryOrder;
  if Assigned(FOnQuoteNotify) then
    FOnQuoteNotify(Self, spSub);
end;

procedure TSPOrder.OnModifyOrderLog(Sender: TObject; Value: String);
begin
  DoLog( Value, true );
end;

procedure TSPOrder.OnMustOrderTimer(Sender: TObject);
begin
  //
  DoMustOrder( ptLong );
  DoMustOrder( ptShort );

end;

function TSPOrder.CheckAccVolumeDiff(aType: TAutoOrderType): boolean;
var
  dDiff, dAcc : double;
begin
  if (FPositions[spSub] = nil) or (FPositions[spMajor] = nil) then
    Result := true
  else begin
    if aType = aoEntry then begin
      dDiff := abs(FPositions[spSub].BuyVol - FPositions[spMajor].SellVol);
      dAcc  := FPositions[spSub].BuyVol;
    end else
    begin
      dDiff := abs(FPositions[spSub].SellVol - FPositions[spMajor].BuyVol);
      dAcc  := FPositions[spSub].SellVol;
    end;

    if (dDiff <= ZERO_EPSILON) or (dAcc <= ZERO_EPSILON) then
      Result := true
    else begin

      if dDiff <= MIN_ORD_AMT then
        Result := true
      else begin
        Result := false;
        DoLog( Format('%s 체결차 %.4n -> 주 체결대기', [
          ifThenStr( aType = aoEntry, '진입', '청산'), dDiff ]), true);
      end;
    end;
  end;
end;

procedure TSPOrder.CheckAutoPutOrder( aType : TAutoOrderType; aOrder : TOrder );
var
  i : integer;
  ord : TOrder;
begin
  for I := 0 to FOrderUnit[aType].OrderList[spSub].Count-1 do
  begin
    ord := FOrderUnit[aType].OrderList[spSub].Orders[i];
    if ord = aOrder then
      if ( ord.OrderType = otCancel ) and ( ord.State = osCanceled ) then
      begin
        FOrderUnit[aType].OrderList[spSub].Delete(i);
        break;
      end;
  end;

  FOrderUnit[aType].CheckOrderList(spSub);

  if FOrderUnit[aType].OrderList[spSub].Count <= 0 then
  begin
//    FReady[aType] := false;
    // 섭거래소 체결이면...체크 해야 할 부분이 있다.
//    if aOrder.FilledQty > 0 then begin
//      if GetAccVolumeGap < 0.000000001 then
//        SetReady(aType, false );
//    end else
    if CheckAccVolumeDiff(aType) then
      SetReady(aType, false );
    DoLog( Format('%s 전체취소' , [ IfThenStr(aType=aoEntry,'진입', '청산') ]), true );
  end;
end;

function TSPOrder.GetAccVolumeGap: double;
begin
  if (FPositions[spSub] = nil) or (FPositions[spMajor] = nil) then
    Result := 0
  else
    Result := abs(FPositions[spSub].BuyVol + FPositions[spMajor].BuyVol -
      FPositions[spSub].SellVol - FPositions[spMajor].SellVol);
end;

function TSPOrder.GetActiveOrderQty( aType : TAutoOrderType; sType : TSPType; iSide : integer ) : double;
var
  aOrder : TOrder;
  i : integer;
begin
  Result := 0.0;
  for I := 0 to FOrderUnit[aType].OrderList[sType].Count-1 do
  begin
    aOrder :=  FOrderUnit[aType].OrderList[sType].Orders[i];
    if aOrder <>nil then
      if ( aOrder.Side = iSide ) and  (aOrder.State = osActive) then
        Result := Result + aOrder.ActiveQty
      else if ( aOrder.Side = iSide ) and (aOrder.State in [osReady, osSent, osSrvAcpt] ) then
        Result := Result + aOrder.OrderQty;
  end;
end;



function TSPOrder.CheckEntryLimit( const dOrderQty : double ): boolean;
var
  aAsset : TAsset;
  dAvailable, dOrderPrice, dOrderAmt,
    dQty, dQty2, dAmt, dAmt2, dTmp, dFee : double;

  i : integer;
  stLog, stTmp : string;
  res : TValueRelationship;
begin
  Result := false;

  try

    if ( FBasePrice[aoEntry] * dOrderQty ) <= MIN_ORD_AMT then
    begin
      stLog := '30';// format('진입 : 최소주문가능금액보 커야 함 %.8f', [dOrderQty] ) ;
      Exit;
    end;

    // 1. 주문가능 금액을 구하고 그 금액으로 주문가능 계약수를 구한다.
    with App.Engine.TradeCore.Orders[FAccounts[spSub].ExchangeKind] do
    begin
      // 섭거래소 나가 있는 매수 미접수/미체결 주문 금액도 도 포함해서 ..체크
      dAmt := GetAcceptOrderAmt(FAccounts[spSub], 1) ;
      dAmt2:= GetUnAcceptOrderAmt(FAccounts[spSub], 1);
    end;

    dAvailable  := FAccounts[spSub].Asset.Balance - dAmt - dAmt2;
    //-------------------
//    dTmp := SimpleRoundTo(dAvailable / FOrderPrice[aoEntry] , -4 );
//    if ((dTmp * FOrderPrice[aoEntry]) > MIN_ORD_AMT) and ( dTmp < dOrderQty ) then
//    begin
////      DoLog( format('진입 : 주문수량 교체 %.4n -> %.4n  (%.0n / %s)  ',
////        [ dOrderQty, dTmp, dAvailable, FSymbols[spSub].PriceToStr(FOrderPrice[aoEntry]) ] ) );
//      stTmp :=  format('주문수량 교체 %.4n -> %.4n', [ dOrderQty, dTmp] );
//      dOrderQty := dTmp;
//    end;
    //-------------------
    dFee:= App.Engine.ApiConfig.ExchangeInfo[ integer(FAccounts[spSub].ExchangeKind) ].TradeFee[FSymbols[spSub].Spec.Market];
    dOrderAmt   := (FOrderPrice[aoEntry] * dOrderQty) * ( 1+(dFee/100))  ;

    res  := CompareValue( dAvailable, dOrderAmt );
    if res <= 0 then
    begin
      stLog := '40';
//      format('진입 : 서브거래소 주문가능 금액 부족 %.0n > %.0n (%.0n, %.0n, %.0n) ', [
//        dOrderAmt,  dAvailable,  FAccounts[spSub].Asset.Balance, dAmt, dAmt2 ]);
      Exit;
    end;

    // 메인 매도 주문이니 잔고 가 있어야 한다..
    aAsset  := FAssets[spMajor];// FAccounts[spMajor].Assets.Find(FParam.Code);
    if aAsset = nil then begin stLog :='진입 : 주거래소 매도 잔고없음'; Exit; end;

    dQty := 0;    dQty2 := 0;
    // 섭거래소 나가 있는 매수 주문도 포함해서 ..체크
    with App.Engine.TradeCore.Orders[FAccounts[spSub].ExchangeKind] do
      dQty := GetActiveOrderQty(FAccounts[spSub], FSymbols[spSub], 1 ) +
              GetUnActiveOrderQty(FAccounts[spSub], FSymbols[spSub], 1 );
    // 주거래소 나가 있는 매도 주문도 포함해서 ..체크
    with App.Engine.TradeCore.Orders[FAccounts[spMajor].ExchangeKind] do
      dQty2:= GetActiveOrderQty(FAccounts[spMajor], FSymbols[spMajor], -1 ) +
              GetUnActiveOrderQty(FAccounts[spMajor], FSymbols[spMajor], -1 );

    res := CompareValue( aAsset.Balance, dOrderQty + dQty + dQty2 );
    //if aAsset.Balance < FParam.EntryQty then begin
    if res < 0 then
    begin
      stLog := '50';
//        format('진입 : 주거래소 매수 잔고 부족 %4n < %.4n + %.4n + %.4n)', [
//         aAsset.Balance, dOrderQty, dQty, dQty2   ]);
      Exit;
    end;

    stTmp := Format('진입 : 주거래소 매수잔고 ( %.4n > %.4n + %.4n + %.4n)  ',
      [ aAsset.Balance, dOrderQty, dQty, dQty2 ]);

    // 서브 거래서 잔고 한도 체크..
    aAsset  := FAssets[spSub];// FAccounts[spSub].Assets.Find(FParam.Code);
    if aAsset = nil then
      aAsset  := FAccounts[spSub].Assets.New( FParam.Code, FAccounts[spSub] );

    // 매수잔고 + 미체결주문 + 나가야할주문 > 매수잔고 한도
    //dQty  := App.Engine.TradeCore.Orders[FAccounts[spSub].ExchangeKind].GetActiveOrderQty(FAccounts[spSub], FSymbols[spSub], 1);
    // 진입, 서브거래소, 매수  주문 수량을 가져와라.
    dQty := GetActiveOrderQty( aoEntry, spSub, 1 );
    dTmp := FParam.LimitEntryPos - FPositions[spSub].Volume - dQty;
    res  := CompareValue( dTmp, dOrderQty  );
    //if dTmp < FParam.EntryQty then
    if res < 0 then
    begin
      stLog := '60';//'진입 : 한도';
      Exit;
    end;

    DoLog( Format('%s  서브 주문가능금액 -> %.0n < %.0n (%.0n, %.0n, %.0n) ', [ stTmp,
        dOrderAmt,  dAvailable,  FAccounts[spSub].Asset.Balance, dAmt, dAmt2 ]), false );

    Result := true;
  finally
    if (not Result) and Assigned( FOnSPNotify2 ) then
      FOnSPNotify2( Self, stLog );
//    if not Result then
//      DoLog( stLog  );
  end;
end;

function TSPOrder.CheckLiquidLimit( const dOrderQty : double ): boolean;
var
  aAsset : TAsset;
  dAvailable, dOrderPrice, dOrderAmt, dQty, dFee,
    dTmp, dAmt, dAmt2 : double;
  i : integer;
  stLog, stTmp : string;
  res : TValueRelationship;
begin
  Result := false;

  try

    if ( FBasePrice[aoExit] * dOrderQty ) <= MIN_ORD_AMT then
    begin
      stLog := '-30';//format('청산 : 최소주문가능금액보 커야 함 %.8f', [dOrderQty]);
      Exit;
    end;
    // 주거래소 주문가능 금액 체크
    // 1. 잔고 -> balance
    // 2. 나가 있는 주문, 나가려고 하는 주문 -> 주거래소 미접수, 미체결
    // 3. 나가야 되는 주문.. -> 섭거래소 청산 주문..

    // 2번 체크
    with App.Engine.TradeCore.Orders[FAccounts[spMajor].ExchangeKind] do
      dAmt := GetAcceptOrderAmt(FAccounts[spMajor], 1) +
              GetUnAcceptOrderAmt(FAccounts[spMajor], 1);

    // 3번 체크
    dFee:= App.Engine.ApiConfig.ExchangeInfo[ integer(FAccounts[spMajor].ExchangeKind) ].TradeFee[FSymbols[spMajor].Spec.Market];
    with App.Engine.TradeCore.Orders[FAccounts[spSub].ExchangeKind] do
    begin
      dAmt2 := GetActiveOrderAmt(FAccounts[spSub], stSPOrder, -1) +
              GetUnActiveOrderAmt(FAccounts[spSub], stSPOrder, -1);
//      dAmt2 := (dQty * FBasePrice[aoExit])*(1+(dFee/100));
    end;

    dAvailable  := FAccounts[spMajor].Asset.Balance - dAmt - dAmt2;

    //-------------------
//    dTmp := SimpleRoundTo( dAvailable / FBasePrice[aoExit] , -4 );
//    if ((dTmp * FBasePrice[aoExit]) > MIN_ORD_AMT) and ( dTmp < dOrderQty ) then
//    begin
////      DoLog( format('청산 : 주문수량 교체 %.4n -> %.4n  (%.0n / %s)  ',
////        [ dOrderQty, dTmp, dAvailable, FSymbols[spMajor].PriceToStr(FBasePrice[aoExit]) ] ) );
//      stTmp :=  format('주문수량 교체 %.4n -> %.4n', [ dOrderQty, dTmp] );
//      dOrderQty := dTmp;
//    end;
    //-------------------

    dOrderAmt := (FBasePrice[aoExit] * dOrderQty)*(1+(dFee/100)) ;
    res  := CompareValue( dAvailable, dOrderAmt );
    //if dAvailable < dOrderAmt then
    if res <= 0 then
    begin
      stLog := '-40';//'청산 : 주거래소 주문가능 금액 부족';
      Exit;
    end;

    // 서브 매도 주문이니 잔고 가 있어야 한다..
    aAsset  := FAssets[spSub];//FAccounts[spSub].Assets.Find(FParam.Code);
    if aAsset = nil then begin stLog := '청산 : 서브거래소 매도 잔고 없음'; Exit; end;

    dQty := 0;
    // 섭거래소 나가 있는 매도 주문도 포함해서 ..체크
    with App.Engine.TradeCore.Orders[FAccounts[spSub].ExchangeKind] do
    begin
      dQty := GetActiveOrderQty(FAccounts[spSub], FSymbols[spSub], -1 ) +
              GetUnActiveOrderQty(FAccounts[spSub], FSymbols[spSub], -1 );
    end;
    res := CompareValue( aAsset.Balance, dOrderQty + dQty );

    //res := CompareValue( aAsset.Balance, dOrderQty );
    //if aAsset.Balance < FParam.LiquidQty  then
    if res < 0 then
    begin
      stLog := '-50';// '청산 : 서브거래소 매수 잔고 부족';
      Exit;
    end;

    stTmp := Format('청산 : 섭거래소 매수잔고 ( %.4f > %.4f + %.4f )',
      [ aAsset.Balance, dOrderQty, dQty ]);

    // 메인 거래서 잔고 한도 체크..
    aAsset  := FAssets[spMajor];//FAccounts[spMajor].Assets.Find(FParam.Code);
    if aAsset = nil then
      aAsset := FAccounts[spMajor].Assets.New(FParam.Code,FAccounts[spMajor]);

    //dQty  := App.Engine.TradeCore.Orders[FAccounts[spMajor].ExchangeKind].GetActiveOrderQty(FAccounts[spMajor], FSymbols[spMajor], 1);

    // 주거래소의 매수 잔고 한도
    // 청산, 주거래소, 매수 주문 수량을 가져와라.
    dQty := GetActiveOrderQty( aoExit, spMajor, 1 );
    dTmp := FParam.LimitLiquidPos - FPositions[spMajor].Volume - dQty ;
    res  := CompareValue( dTmp, dOrderQty );
    //if dTmp < FParam.LiquidQty then
    if res < 0 then
    begin
      stLog := '-60';// '청산 : 한도';
      Exit;
    end;

    DoLog( Format('%s : 주거래소 주문가능금액 -> %.0n < %.0n (%.0n, %.0n, %.0n, %.2f) ', [stTmp,
      dOrderAmt,  dAvailable,  FAccounts[spMajor].Asset.Balance, dAmt, dAmt2, dFee]), false);

    Result := true;
  finally
    if (not Result) and Assigned( FOnSPNotify2 ) then
      FOnSPNotify2( Self, stLog );
    // 화면표시 X,   파일에만 남김.
//    if not Result then
//      DoLog(stLog );
  end;
end;

function TSPOrder.CheckMajorAvailableAmt( dOrderPrice, dOrderQty : double ) : boolean;
var
  dAmt, dAmt2, dQty, dOrderAmt, dAvailable, dFee : double;
  res : TValueRelationship;
begin

  with App.Engine.TradeCore.Orders[FAccounts[spMajor].ExchangeKind] do
    dAmt := GetAcceptOrderAmt(FAccounts[spMajor], 1) +
            GetUnAcceptOrderAmt(FAccounts[spMajor], 1);
  // 3번 체크
  dFee:= App.Engine.ApiConfig.ExchangeInfo[ integer(FAccounts[spMajor].ExchangeKind) ].TradeFee[FSymbols[spMajor].Spec.Market];
  with App.Engine.TradeCore.Orders[FAccounts[spSub].ExchangeKind] do
  begin
    dAmt2 := GetActiveOrderAmt(FAccounts[spSub], stSPOrder, -1) +
            GetUnActiveOrderAmt(FAccounts[spSub], stSPOrder, -1);
    //dAmt2 := (dQty * dOrderPrice)*(1+(dFee/100));
  end;

  dAvailable  := FAccounts[spMajor].Asset.Balance - dAmt - dAmt2;

  dOrderAmt := (dOrderPrice * dOrderQty)*(1+(dFee/100)); ;
  res  := CompareValue( dAvailable, dOrderAmt );

  if res <= 0 then begin
    Exit (false);
  end;

  Result := true;
end;

function TSPOrder.CheckMajorAvailableBalance( dOrderQty : double ) : boolean;
var
  dQty, dQty2 : double;
  res : TValueRelationship;
begin
  // 섭거래소 나가 있는 매수 주문도 포함해서 ..체크
  with App.Engine.TradeCore.Orders[FAccounts[spSub].ExchangeKind] do
    dQty := GetActiveOrderQty(FAccounts[spSub], FSymbols[spSub], 1 ) +
            GetUnActiveOrderQty(FAccounts[spSub], FSymbols[spSub], 1 );
    // 주거래소 나가 있는 매도 주문도 포함해서 ..체크
  with App.Engine.TradeCore.Orders[FAccounts[spMajor].ExchangeKind] do
    dQty2:= GetActiveOrderQty(FAccounts[spMajor], FSymbols[spMajor], -1 ) +
            GetUnActiveOrderQty(FAccounts[spMajor], FSymbols[spMajor], -1 );

  res := CompareValue( FAssets[spMajor].Balance, dOrderQty + dQty + dQty2 );

  if res < 0 then begin
    Exit (false);
  end;

  Result := true;
end;


procedure TSPOrder.SetReady( aType : TAutoOrderType; bValue : boolean) ;
begin
  if FParam.UseMdodifyDelay then
  begin
    FTimer[aType].Interval  := Round(FParam.ModifyDelaySec * 1000);
    FTimer[aType].Enabled := true;
    FLastOrdQty[aType]    := 0;
//    DoLog( Format('--- %s Flag=false 타이머 시작 (%d) ---' , [ ifthenStr(aType=aoEntry,'진입','청산'),
//       FOrderUnit[aType].OrderList[spSub].Count] ) , true );
  end
  else begin
    FReady[aType] := bValue;
    FLastOrdQty[aType]    := 0;
    DoLog( Format('%s 섭주문 재개 (%d / %d)  No timer' , [ ifthenStr(aType=aoEntry,'진입','청산'),
       FOrderUnit[aType].OrderList[spSub].Count, FOrderUnit[aType].OrderList[spMajor].Count ]) , true );
  end;
end;

procedure TSPOrder.OnOrder(aOrder: TOrder; iEventID : integer);
var
  dPrice : double;
  iTick  : integer;
  aNewOrder : TOrder;
  aModOrder : TAutoModifyOrder;
begin

  if aOrder.GroupNo <> FName then
  begin
    Exit;
  end;

  // 매수, 매도, 종목에 따라 처리 분기
  if aOrder.Symbol = FSymbols[spMajor] then
  begin
    var aType : TAutoOrderType;
    if aOrder.Side > 0 then
      aType := aoExit
    else
      aType := aoEntry;

    case aOrder.OrderType of
      otNormal:
        case aOrder.State of
          osActive:
            begin
            //  FModifyOrderList[aoEntry].New( FParam, OnModifyOrderLog ).SetOrder( aOrder, true )  ;
            end;
          osSrvRjt,
          osRejected:
            begin
              DoLog( Format('주거래소 %s 주문 거부 : %s, %s, %s, %s', [ aOrder.OrderTypeToStr,
                ifThenStr(aType=aoEntry,'진입','청산'),
                ifThenStr(aOrder.Side >0 , '매수','매도'), aOrder.Symbol.PriceToStr( aOrder.Price ),
                aOrder.Symbol.QtyToStr( aOrder.OrderQty ) ] ), true);

              // 거부면 어떻게 하지?...
              if aOrder.OrderType = otNormal then begin
                if aOrder.Side > 0  then
                  MustOrdQty[ptLong] := MustOrdQty[ptLong] + aOrder.OrderQty
                else
                  MustOrdQty[ptShort] := MustOrdQty[ptShort] + aOrder.OrderQty;

                DoLog( Format('거부로 인한 체결차 발생 -> (S:%s,L:%s)', [
                  FSymbols[spMajor].QtyToStr(MustOrdQty[ptShort])
                  , FSymbols[spMajor].QtyToStr(MustOrdQty[ptLong]) ])  , true);
              end;
            end;
        end;

      otCancel:
        if aOrder.State = osCanceled then
        begin
          // 취소한 주문이 주거래소 체결시키기 위한 주문이면
          // 바로 주문을 낸다.
          aModOrder := FModifyOrderList[aType].Find(aOrder);
          if aModOrder <> nil then
          begin

            var dOrdPrice : double;
            // 체결 되게
            if aOrder.Side > 0 then begin
              if FParam.UseHogaMerge then
                dOrdPrice := FMgQuotes[spMajor].Asks[0].Price
              else
                dOrdPrice := FSymbols[spMajor].Asks[0].Price;
            end else
            begin
              if FParam.UseHogaMerge then
                dOrdPrice := FMgQuotes[spMajor].Bids[0].Price
              else
                dOrdPrice := FSymbols[spMajor].Bids[0].Price;
            end;

            // 체결 안되게
//            if aOrder.Side > 0 then begin
//              if FParam.UseHogaMerge then
//                dOrdPrice := FMgQuotes[spMajor].Bids[0].Price
//              else
//                dOrdPrice := FSymbols[spMajor].Bids[0].Price;
//            end else
//            begin
//              if FParam.UseHogaMerge then
//                dOrdPrice := FMgQuotes[spMajor].Asks[0].Price
//              else
//                dOrdPrice := FSymbols[spMajor].Asks[0].Price;
//            end;

            if aModOrder.Count >= FParam.AutoModifyCount then begin
              if aType = aoEntry then
                iTick := FParam.AutoModifyLastTick * -1
              else
                iTick := FParam.AutoModifyLastTick;

              dPrice := TicksFromPrice( aOrder.Symbol, dOrdPrice, iTick)
            end
            else
              dPrice := dOrdPrice;

            aNewOrder := DoOrder( aOrder.Account, aOrder.Symbol, aOrder.Side, dPrice, aOrder.CanceledQty, aType = aoEntry);
            if aNewOrder <> nil then begin
              DoLog(format('%s : 주거래소로 정정 %d.th 주문', [ ifThenStr(aType=aoEntry,'진입','청산'), aModOrder.Count]), true);
              FOrderUnit[aType].OrderList[spMajor].Add( aNewOrder );
              aModOrder.SetOrder( aNewOrder, true);
            end;
          end;
        end;
    end;

    FModifyOrderList[aType].CheckOrder;

  end else
  if aOrder.Symbol = FSymbols[spSub] then
  begin
    if aOrder.Side > 0  then begin
      //if not FParam.UseEntry then Exit;
      // 진입...까는 주문
      CheckAutoPutOrder( aoEntry, aOrder );
      FLastOrdQty[aoEntry]  := aOrder.ActiveQty + aOrder.FilledQty;
    end else
    begin
      //if not FParam.UseLiquid then Exit;
      // 청산...까는 주문
      CheckAutoPutOrder( aoExit, aOrder );
      FLastOrdQty[aoExit]  := aOrder.ActiveQty + aOrder.FilledQty;
    end;
  end
  else
    Exit;
end;

procedure TSPOrder.OnFill(aOrder: TOrder);
var
  ord, newOrd : TOrder;
  i   : integer;
  dQty  : double;
  aType : TAutoOrderType;
  sType : TSPType;
  bOrder : boolean;
begin

  // 매수, 매도, 종목에 따라 처리 분기
  if aOrder.Symbol = FSymbols[spMajor] then
  begin
    if aOrder.Side > 0  then begin
      // 청산...치는 주문
      aType := aoExit;
    end else
    begin
      // 진입.. 치는 주문
      aType := aoEntry;
    end;
    sType := spMajor;
  end
  else if aOrder.Symbol = FSymbols[spSub] then
  begin
    if aOrder.Side > 0  then begin
      // 진입...까는 주문
      //CheckAutoPutOrder( aoEntry, aOrder );
      aType := aoEntry;
    end else
    begin
      // 청산...까는 주문
      //CheckAutoPutOrder( aoExit, aOrder );
      aType := aoExit;
    end;
    sType := spSub;
  end else
    Exit;

  ord := FOrderUnit[aType].OrderList[sType].FindOrder( aOrder );
//  ord := FOrderUnit[aType].OrderList[sType].FindOrder( aOrder.OrderNo );
  if ord = nil then Exit;

  // 서브거래소 주문이 체결 되면..주거래소로 주문.
  with ord.Fills do
  begin
    DoLog( Format('%s 주문 체결-> %s,[ %s, %s ]', [ ifThenStr(aType=aoEntry,'진입','청산'),
      ord.Represent,
      aOrder.Symbol.PriceToStr( LastFill.Price ),   aOrder.Symbol.QtyToStr( LastFill.Volume )
     ] ), true);

    var dPrice, dAmt : double;
    dPrice :=  FBasePrice[aType];

//    // 테스트 : 체결 안되게
//    if ord.Side < 0 then
//      dPrice := FSymbols[spMajor].Bids[0].Price
//    else
//      dPrice := FSymbols[spMajor].Asks[0].Price;

    // 주거래소로 주문..
    if sType = spSub then begin

      bOrder := true;

      if (dPrice * LastFill.Volume) < MIN_ORD_AMT then
      begin
        bOrder := false;
        if aOrder.Side > 0  then begin
          // 섭 매수체결 -> 주 매도주문 수량
          MustOrdQty[ptShort]  := MustOrdQty[ptShort] + LastFill.Volume;
        end
        else begin
          // 섭 매도체결 -> 주 매수주문 수량
          MustOrdQty[ptLong] := MustOrdQty[ptLong] + LastFill.Volume;
        end;

        DoLog( Format('Dust 체결차 발생 -> (S:%s,L:%s)', [
          FSymbols[spMajor].QtyToStr(MustOrdQty[ptShort])
          , FSymbols[spMajor].QtyToStr(MustOrdQty[ptLong]) ])  , true);

      end else
      begin
        dQty   := LastFill.Volume;

        if (aOrder.Side > 0) and (MustOrdQty[ptShort] > 0)  then begin
          dQty := LastFill.Volume + MustOrdQty[ptShort];
          if CheckMajorAvailableBalance(dQty) then begin
            DoLog( Format('체결차 +  주거래소로 주문 %.4n -> %.4n', [LastFill.Volume, dQty]), true );
            MustOrdQty[ptShort] := 0;
          end
          else
            dQty := LastFill.Volume;
        end
        else if (aOrder.Side < 0) and (MustOrdQty[ptLong] >0) then begin
          dQty := LastFill.Volume + MustOrdQty[ptLong];
          if CheckMajorAvailableAmt(dPrice, dQty) then
          begin
            DoLog( Format('체결차 +  주거래소로 주문 %.4n -> %.4n', [LastFill.Volume, dQty]), true );
            MustOrdQty[ptLong] := 0;
          end
          else
            dQty := LastFill.Volume;
        end;
      end;

      if bOrder then
      begin
        newOrd  := DoOrder( FAccounts[spMajor], FSymbols[spMajor],  ifThen(aType=aoEntry,-1,1),
            dPrice, dQty, aType=aoEntry );
            //FSymbols[spMajor].Asks[3].Price, LastFill.Volume, true );
        if newOrd <> nil then begin
          FOrderUnit[aType].OrderList[spMajor].Add( newOrd );
          FModifyOrderList[aType].New( FParam, OnModifyOrderLog ).SetOrder( newOrd, true )  ;
        end;
      end;
    end;
    // 주거래소 누적 거래량.
    //if sType = spMajor then
    AccVolume[sType] := AccVolume[sType] + LastFill.Volume;
  end;

  FOrderUnit[aType].CheckOrderList(sType);
  FModifyOrderList[aType].CheckOrder;

  if FOrderUnit[aType].OrderList[sType].Count <= 0 then
  begin
    if sType = spSub then
    // 주거래소가 전량 체결 되야...섭거래소가 나가게끔 수정
//    if sType = spMajor then
//      SetReady(aType, false );
    DoLog( Format('%s 전량체결 %s' , [ IfThenStr(aType=aoEntry,'진입', '청산'),
      IfThenStr(sType=spMajor,'(주)','(섭)')    ]), true );
  end;

  if FPositions[sType] = nil then Exit;
  FPositions[sType].AddFill( ord.Fills.LastFill );

  if (FOrderUnit[aType].OrderList[spSub].Count <= 0) and (FOrderUnit[aType].OrderList[spMajor].Count <= 0)then
  begin
    if (sType = spMajor) or ((sType = spSub) and (not bOrder)) then
      if CheckAccVolumeDiff(aType) then
      begin
        SetReady(aType, false );
        if sType = spMajor then
          DoLog( Format('%s 전량체결 (주)', [ IfThenStr(aType=aoEntry,'진입', '청산')]), true )
        else
          DoLog( Format('%s 최소금액 체결 (섭)', [ IfThenStr(aType=aoEntry,'진입', '청산')]), true );
      end;
  end;
end;

procedure TSPOrder.OnOrderUnitLog(Sender: TObject; Value: String);
begin
  DoLog( Value, true );
end;

procedure TSPOrder.OnParam(aItem: TSPParam);
begin
  FParam  := aItem;

  if FParam.UseEntry then
  begin
    //FReady[aoEntry] := false;
  end else
  begin
    // 주문 캔슬..
    FReady[aoEntry] := false;
    FOrderUnit[aoEntry].DoCancelOrder(0,'') ;
  end;

  if FParam.UseLiquid then
  begin
//    FReady[aoExit] := false;
  end else
  begin
    // 주문 캔슬..
    FReady[aoExit] := false;
    FOrderUnit[aoExit].DoCancelOrder(0,'') ;
  end;

//  if FSymbols[spMajor] <> nil then
//    FMgQuotes[spMajor].SetSymbol( FSymbols[spMajor], aItem.HogaMergeUnit );
//
//  if FSymbols[spSub] <> nil then
//    FMgQuotes[spSub].SetSymbol( FSymbols[spSub], aItem.HogaMergeUnit );
end;

procedure TSPOrder.OnQuote(aSymbol: TSymbol);
begin
//  if not IsRun then Exit;
//
//  CalcSPValue;
//
//  if aSymbol = FSymbols[spMajor] then
//    OnMajorQuote( aSymbol )
//  else if aSymbol = FSymbols[spSub] then
//    OnSubQuote( aSymbol );

end;

//procedure TSPOrder.QuoteProc(Sender, Receiver: TObject; DataID: Integer;
//  DataObj: TObject; EventID: TDistributorID);
//begin
//  if (Self <> Receiver ) or (DataObj = nil ) then Exit;
//
//  OnQuote(( DataObj as TQuote).Symbol );
//end;

procedure TSPOrder.SetParam(const Value: TSPParam);
begin
  FParam := Value;
end;

function TSPOrder.Start: boolean;
begin

  if ( FSymbols[spMajor] = nil ) or ( FSymbols[spSub] = nil ) then Exit (false);
  if ( FAccounts[spMajor] = nil ) or ( FAccounts[spSub] = nil ) then Exit (false);

  FRun  := true;

//  App.Engine.QuoteBroker.Brokers[FParam.MajorEx].Subscribe( Self, FSymbols[spMajor], QuoteProc);
//  App.Engine.QuoteBroker.Brokers[FParam.SubEx].Subscribe( Self, FSymbols[spSub], QuoteProc);
//
//  App.Engine.TradeBroker.Subscribe( Self, TradeProc );

  FTimer2.Enabled := true;

  Result := true;

  DoLog('Start', true);
end;

procedure TSPOrder.Stop;
begin
  FRun  := false;

  FOrderUnit[aoEntry].DoCancelOrder(0,'');
  FOrderUnit[aoExit].DoCancelOrder(0, '');

  FTimer2.Enabled := false;
//  App.Engine.QuoteBroker.Cancel( Self );
//  App.Engine.TradeBroker.Unsubscribe(Self );

  DoLog('Stop', true);
end;

procedure TSPOrder.TradeProc(Sender, Receiver: TObject; DataID: Integer;
  DataObj: TObject; EventID: TDistributorID);
begin

  if not IsRun then Exit;

  if ( Receiver <> Self ) or ( DataObj = nil ) then Exit;

  case Integer(EventID) of
    ORDER_ACCEPTED,
    ORDER_REJECTED,
    ORDER_CANCELED : OnOrder(DataObj as TOrder, Integer(EventID) );
    ORDER_FILLED: OnFill(DataObj as TOrder);

//    POSITION_UPDATE : OnPosition( DataObj as TPosition);
  end;
end;

{ TSPOrders }

constructor TSPOrders.Create;
begin
  inherited Create( TSPOrder );

  LastGetTime[spMajor] := now;
  LastGetTime[spSub]   := now;
  FSelect := nil;
end;

destructor TSPOrders.Destroy;
begin

  inherited;
end;

function TSPOrders.Find(stCode: string): TSPOrder;
var
  I: Integer;
  aSP : TSPOrder;
begin
  Result := nil;
  for I := 0 to Count-1 do
  begin
    aSP := GetSPOrders(i);
    if (aSP <> nil) and ( aSP.Name = stCode ) then
    begin
      Result := aSP;
      break;
    end;
  end;
end;

function TSPOrders.GetAccVolumeGap: double;
var
  I: Integer;
  aSP : TSPOrder;
begin
  Result := 0;
  for I := 0 to Count-1 do
  begin
    aSP := GetSPOrders(i);
    if aSP <> nil then
      Result := Result + aSP.GetAccVolumeGap;
  end;
end;

function TSPOrders.GetBalance(sType: TSPType): double;
var
  I: Integer;
  aSP : TSPOrder;
begin
  Result := 0;
  for I := 0 to Count-1 do
  begin
    aSP := GetSPOrders(i);
    if aSP.Assets[sType] <> nil then
    begin
      Result := aSP.Assets[sType].Balance;
      break;
    end;
  end;

end;

function TSPOrders.GetSPOrders(i: Integer): TSPOrder;
begin
  if (i < 0 ) or ( i >= Count )  then
    Result := nil
  else
    Result := Items[i] as TSPOrder;
end;

function TSPOrders.GetTotTradeRate: double;
var
  I: Integer;
  aSP : TSPOrder;
  dSum, dPOs: double;
begin
  Result := 0;  dSum := 0;  dPos := 0;
  for I := 0 to Count-1 do
  begin
    aSP := GetSPOrders(i);
    if aSP <> nil then  begin
      if aSP.Run then
        dPos := aSP.BasePosQty;
      dSum := dSum + aSP.AccVolume[spMajor];
    end;
  end;

  if CheckZero( dPos ) then
    Result := 0
  else
    Result := dSum / dPos * 100;
end;

function TSPOrders.GetTotTradeAmtDiff(sType: TSPType): double;
var
  I: Integer;
  aSP : TSPOrder;
begin
  Result := 0;
  for I := 0 to Count-1 do
  begin
    aSP := GetSPOrders(i);
    if aSP <> nil then
      Result := Result + aSP.GetTradeAmtDiff(sType);
  end;
end;

procedure TSPOrders.GetTotalValue(var dValue: array of double);
var
  I: Integer;
  aSP : TSPOrder;
begin

  for i := 0 to High(dValue) do dValue[i] := 0;

//  for I := 0 to Count-1 do
//  begin
//    aSP := GetSPOrders(i);
//    if aSP <> nil then begin
//      dValue[0] := dValue[0] + aSP.GetTradeRate;
//      dValue[1] := dValue[1] + aSP.GetAccVolumeGap;
//      dValue[2] := dValue[2] + aSP.GetTradeAmtDiff(spMajor);
//      dValue[3] := dValue[3] + aSP.GetTradeAmtDiff(spSub);
//      dValue[4] := dValue[4] + aSP.GetTradeAmt(true);
//    end;
//  end;

  for I := 0 to Count-1 do
  begin
    aSP := GetSPOrders(i);
    if aSP <> nil then begin
      if (aSP.Positions[spSub] = nil) or (aSP.Positions[spMajor] = nil) then continue;

      dValue[0] := dValue[0] + aSP.Positions[spSub].BuyVol;// GetTradeRate;
      dValue[1] := dValue[1] + aSP.Positions[spSub].SellVol;
      dValue[2] := dValue[2] + aSP.Positions[spSub].BuyAmt;
      dValue[3] := dValue[3] + aSP.Positions[spSub].SellAmt;

      dValue[4] := dValue[4] + aSP.Positions[spMajor].BuyVol;
      dValue[5] := dValue[5] + aSP.Positions[spMajor].SellVol;
      dValue[6] := dValue[6] + aSP.Positions[spMajor].BuyAmt;
      dValue[7] := dValue[7] + aSP.Positions[spMajor].SellAmt;

      //
      dValue[8] := dValue[8] + aSP.Positions[spMajor].LastPL;
      dValue[9] := dValue[9] + aSP.Positions[spSub].LastPL;
    end;
  end;

end;

function TSPOrders.GetTotFee: double;
var
  I: Integer;
  aSP : TSPOrder;
begin
  Result := 0;
  for I := 0 to Count-1 do
  begin
    aSP := GetSPOrders(i);
    if aSP <> nil then
      Result := Result + aSP.GetTradeFee;
  end;

end;

function TSPOrders.GetTotTradeAmt(bSum: boolean): double;
var
  I: Integer;
  aSP : TSPOrder;
begin
  Result := 0;
  for I := 0 to Count-1 do
  begin
    aSP := GetSPOrders(i);
    if aSP <> nil then
      Result := Result + aSP.GetTradeAmt(bSum);
  end;

end;

function TSPOrders.GetTotTradePL: double;
var
  I: Integer;
  aSP : TSPOrder;
begin
  Result := 0;
  for I := 0 to Count-1 do
  begin
    aSP := GetSPOrders(i);
    if aSP <> nil then
      Result := Result + aSP.GetTradePL;
  end;
end;

function TSPOrders.GetTotTradePLForComp: double;
var
  I: Integer;
  aSP : TSPOrder;
begin
  Result := 0;
  for I := 0 to Count-1 do
  begin
    aSP := GetSPOrders(i);
    if aSP <> nil then
      Result := Result + aSP.GetLastPL;
  end;
end;

//procedure TSPOrders.init(aParam: TSPParam; aNotify: TTextNotifyEvent);
//var
//  i : Integer;
//begin
//  for I := 0 to Count-1 do begin
//    GetSPOrders(i).Init( aParam);
//    GetSPOrders(i).OnSPNotify := aNotify;
//  end;
//end;

function TSPOrders.New(idx: integer): TSPOrder;
begin
  Result := Add as TSPOrder;
  Result.No := idx;
  Result.Name := Format('SP_%3.3d', [ idx ] );
end;

procedure TSPOrders.OnTrade(aData: TObject; iEvent: integer);
var
  aOrder : TOrder;
  i : integer;
begin

  aOrder := aData as TOrder;
  if aOrder.StgType <> stSPOrder then Exit;

  for I := 0 to Count-1 do
    case iEvent of
      ORDER_ACCEPTED,
      ORDER_REJECTED,
      ORDER_CANCELED : GetSPOrders(i).OnOrder(aOrder, iEvent );
      ORDER_FILLED: GetSPOrders(i).OnFill(aOrder);
    end;
end;

function TSPOrders.New(stCode: string): TSPOrder;
begin
  Result := Add as TSPOrder;
  Result.Name   := stCode;
end;


function TSPOrders.IsWaiting(sType : TSPType): boolean;
var
  iTime : integer;
begin

  Result := false;

  iTime := MilliSecondsBetween( now, LastGetTime[sType] );
  if iTime > 100 then
    Result := true;

end;

procedure TSPOrders.SetTime(sType : TSPType);
begin
  LastGetTime[sType] := now;
end;



end.
