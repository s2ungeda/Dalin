unit UUpbitWithdraw;

interface

uses
  System.Classes, System.SysUtils,

  UApiWithdraw,

  UApiTypes, UApiConsts, UTypes

  ;

type

  TDpItem = class
  public
    Code : string;
    UUid : string;
    Amount : string;
    Time   : TDateTime;
  end;

  TUpbitWithdraw = class( TApiWithdraw )
  private
    FDpList : array [0..1] of TList;

    procedure DoWithdrawAble;
    procedure ReuqestWithdraw;
    procedure DoWithdraw; override;
    procedure Parse(const aType, aData: string);
    procedure DoUpdateAsset;
    function CompareLists: Boolean;
  public
    Constructor Create; override;
    Destructor  Destroy; override;
    procedure RecvRequest(Sender : TObject);  override;
    procedure RequestWithdrawAddress(sCurrency, sNetType: string); override;
    procedure RequestDepositList(sCurrency: string; bFirst : boolean); override;
    procedure RequestDepositAddress(sCurrency, sNetType: string); override;
    // 주소 생성 (국내 거래소만)
    procedure RequestNewDepositAddress(sCurrency, sNetType: string); override;
  end;

implementation

uses
  GApp, GLibs
  , UBithSpot
  , REST.Types
  , System.JSON, System.Math
  , IdGlobal
  , idcodermime
  , Web.HTTPApp
  , UEncrypts
  , System.Hash
  , JOSE.Core.JWT,
  JOSE.Core.JWK,
  JOSE.Core.JWS,
  JOSE.Core.JWA,
  JOSE.Core.Builder,
  JOSE.Types.JSON,
  JOSE.Encoding.Base64
  ;

{ TBitWithdraw }


constructor TUpbitWithdraw.Create;
begin
  inherited Create;
  FAddress  := TStringList.Create;
  FDPList[0]:= TList.Create;
  FDPList[1]:= TList.Create;
end;

destructor TUpbitWithdraw.Destroy;
begin
  FAddress.free;
  FDPList[0].Free;
  FDPList[1].Free;
  inherited;
end;

procedure TUpbitWithdraw.DoUpdateAsset;
begin
  App.Engine.TradeCore.Accounts[ekUpbit].WidthDrawCoin(ekUpbit, UpperCase(Param.currency), Param.qty);
  App.Log(llInfo, '업비트 : %s', [Param.Represent]);
end;

procedure TUpbitWithdraw.DoWithdraw;
begin
  DoWithdrawAble;

end;

procedure TUpbitWithdraw.DoWithdrawAble;
var
  LToken: TJWT;
  guid : TGUID;
  sSig, sID, sToken, sOut, sJson, sRsrc : string;
  vHash : THashSHA2;
begin

  LToken:= TJWT.Create(TJWTClaims);
  try
    sID := GetUUID;

    sJson  := format('currency=%s&net_type=%s', [ Param.currency, Param.nettype]) ;
    sOut  := vHash.gethashstring( sJson, SHA512 );

    LToken.Claims.SetClaimOfType<string>('access_key', App.Engine.ApiConfig.GetApiKey( ekUpbit, eaSpot));
    LToken.Claims.SetClaimOfType<string>('nonce', sID );
    LToken.Claims.SetClaimOfType<string>('query_hash', sOut );

    LToken.Claims.SetClaimOfType<string>('query_hash_alg', 'SHA512' );

    Req.Client.Params.Clear;
    Req.Req.Params.Clear;

    sSig := TJOSE.SerializeCompact(App.Engine.ApiConfig.GetSceretKey(ekUpbit, eaSpot),  TJOSEAlgorithmId.HS512, LToken);
    sToken := Format('Bearer %s', [sSig ]);

    with Req do
      Req.AddParameter( 'Authorization', sToken, TRESTRequestParameterKind.pkHTTPHEADER, [poDoNotEncode] );

    sRsrc := '/v1/withdraws/chance?'+sJson;
    Req.init(rmGET, 'https://api.upbit.com', sRsrc, 'status' )  ;

    Req.RequestAsync;
  finally
    LToken.Free;
  end;

end;

procedure TUpbitWithdraw.RequestDepositAddress(sCurrency, sNetType: string);
var
  LToken: TJWT;
  guid : TGUID;
  sSig, sID, sToken, sOut, sJson, sRsrc : string;
  vHash : THashSHA2;
begin

  LToken:= TJWT.Create(TJWTClaims);
  try
    sID := GetUUID;

    sJson  := format('currency=%s&net_type=%s', [sCurrency, sNetType]) ;
    sOut  := vHash.gethashstring( sJson, SHA512 );

    LToken.Claims.SetClaimOfType<string>('access_key', App.Engine.ApiConfig.GetApiKey( ekUpbit, eaSpot));
    LToken.Claims.SetClaimOfType<string>('nonce', sID );
    LToken.Claims.SetClaimOfType<string>('query_hash', sOut );

    LToken.Claims.SetClaimOfType<string>('query_hash_alg', 'SHA512' );

    Req.Client.Params.Clear;
    Req.Req.Params.Clear;

    sSig := TJOSE.SerializeCompact(App.Engine.ApiConfig.GetSceretKey(ekUpbit, eaSpot),  TJOSEAlgorithmId.HS512, LToken);
    sToken := Format('Bearer %s', [sSig ]);

    with Req do
      Req.AddParameter( 'Authorization', sToken, TRESTRequestParameterKind.pkHTTPHEADER, [poDoNotEncode] );

    sRsrc := '/v1/deposits/coin_address?'+sJson;
    Req.init(rmGET, 'https://api.upbit.com', sRsrc, 'depositaddr' )  ;
    Req.Field1  := sCurrency;
    Req.Field2  := sNetType;
    Req.RequestAsync;
  finally
    LToken.Free;
  end;

end;

procedure TUpbitWithdraw.RequestDepositList(sCurrency: string; bFirst : boolean);
var
  LToken: TJWT;
  guid : TGUID;
  sSig, sID, sToken, sOut, sJson, sRsrc : string;
  vHash : THashSHA2;
begin

  LToken:= TJWT.Create(TJWTClaims);
  try
    sID := GetUUID;

    sJson  := format('currency=%s&limit=2', [sCurrency]) ;
    sOut  := vHash.gethashstring( sJson, SHA512 );

    LToken.Claims.SetClaimOfType<string>('access_key', App.Engine.ApiConfig.GetApiKey( ekUpbit, eaSpot));
    LToken.Claims.SetClaimOfType<string>('nonce', sID );
    LToken.Claims.SetClaimOfType<string>('query_hash', sOut );

    LToken.Claims.SetClaimOfType<string>('query_hash_alg', 'SHA512' );

    Req.Client.Params.Clear;
    Req.Req.Params.Clear;

    sSig := TJOSE.SerializeCompact(App.Engine.ApiConfig.GetSceretKey(ekUpbit, eaSpot),  TJOSEAlgorithmId.HS512, LToken);
    sToken := Format('Bearer %s', [sSig ]);

    with Req do
      Req.AddParameter( 'Authorization', sToken, TRESTRequestParameterKind.pkHTTPHEADER, [poDoNotEncode] );

    sRsrc := '/v1/deposits?'+sJson;
    Req.init(rmGET, 'https://api.upbit.com', sRsrc, 'deposit' )  ;
    Req.Field1  := sCurrency;
    Req.Field2  := ifThenStr( bFirst, 'first', 'multi');

    Req.RequestAsync;
  finally
    LToken.Free;
  end;
end;

procedure TUpbitWithdraw.RequestNewDepositAddress(sCurrency, sNetType: string);
var
  LToken: TJWT;
  guid : TGUID;
  sSig, sID, sToken, sOut, sJson, sRsrc : string;
  vHash : THashSHA2;
begin

  LToken:= TJWT.Create(TJWTClaims);
  try
    sID := GetUUID;

    sJson  := format('currency=%s&net_type=%s', [sCurrency, sNetType]) ;
    sOut  := vHash.gethashstring( sJson, SHA512 );

    LToken.Claims.SetClaimOfType<string>('access_key', App.Engine.ApiConfig.GetApiKey( ekUpbit, eaSpot));
    LToken.Claims.SetClaimOfType<string>('nonce', sID );
    LToken.Claims.SetClaimOfType<string>('query_hash', sOut );

    LToken.Claims.SetClaimOfType<string>('query_hash_alg', 'SHA512' );

    Req.Client.Params.Clear;
    Req.Req.Params.Clear;

    sSig := TJOSE.SerializeCompact(App.Engine.ApiConfig.GetSceretKey(ekUpbit, eaSpot),  TJOSEAlgorithmId.HS512, LToken);
    sToken := Format('Bearer %s', [sSig ]);

    with Req do
      Req.AddParameter( 'Authorization', sToken, TRESTRequestParameterKind.pkHTTPHEADER, [poDoNotEncode] );

    sRsrc := '/v1/deposits/generate_coin_address?'+sJson;
    Req.init(rmPOST, 'https://api.upbit.com', sRsrc, 'depositaddr' )  ;
    Req.Field1  := sCurrency;
    Req.Field2  := sNetType;
    Req.RequestAsync;
  finally
    LToken.Free;
  end;

end;

procedure TUpbitWithdraw.RequestWithdrawAddress(sCurrency, sNetType: string);
var
  LToken: TJWT;
  guid : TGUID;
  sSig, sID, sToken, sOut, sJson, sRsrc : string;
  vHash : THashSHA2;
begin

  if (sCurrency ='') or (sNetType='') then Exit;

  LToken:= TJWT.Create(TJWTClaims);
  try
    sID := GetUUID;

//    sJson  := format('currency=%s&net_type=%s', [ Param.currency, Param.nettype]) ;
//    sOut  := vHash.gethashstring( sJson, SHA512 );

    LToken.Claims.SetClaimOfType<string>('access_key', App.Engine.ApiConfig.GetApiKey( ekUpbit, eaSpot));
    LToken.Claims.SetClaimOfType<string>('nonce', sID );
//    LToken.Claims.SetClaimOfType<string>('query_hash', sOut );

    LToken.Claims.SetClaimOfType<string>('query_hash_alg', 'SHA512' );

    Req.Client.Params.Clear;
    Req.Req.Params.Clear;

    sSig := TJOSE.SerializeCompact(App.Engine.ApiConfig.GetSceretKey(ekUpbit, eaSpot),  TJOSEAlgorithmId.HS512, LToken);
    sToken := Format('Bearer %s', [sSig ]);

    with Req do
      Req.AddParameter( 'Authorization', sToken, TRESTRequestParameterKind.pkHTTPHEADER, [poDoNotEncode] );

    sRsrc := '/v1/withdraws/coin_addresses';//?'+sJson;
    Req.init(rmGET, 'https://api.upbit.com', sRsrc, 'address' )  ;
    Req.Field1  := sCurrency;
    Req.Field2  := sNetType;

//    Req.Request(outJson, outRes);;
  finally
    LToken.Free;
  end;

end;

procedure TUpbitWithdraw.ReuqestWithdraw;
var
  LToken: TJWT;
  guid : TGUID;
  sSig, sID, sToken, sOut, sJson, sRsrc : string;
  vHash : THashSHA2;
begin

  LToken:= TJWT.Create(TJWTClaims);
  try
    sID := GetUUID;
    sJson  := format('currency=%s&net_type=%s&amount=%s&address=%s&secondary_address=%s',
      [Param.currency, Param.nettype, Param.qty, Param.addr1, Param.addr2 ]) ;

    sOut  := vHash.gethashstring( sJson, SHA512 );

    LToken.Claims.SetClaimOfType<string>('access_key', App.Engine.ApiConfig.GetApiKey( ekUpbit, eaSpot));
    LToken.Claims.SetClaimOfType<string>('nonce', sID );
    LToken.Claims.SetClaimOfType<string>('query_hash', sOut );

    LToken.Claims.SetClaimOfType<string>('query_hash_alg', 'SHA512' );

    Req.Client.Params.Clear;
    Req.Req.Params.Clear;

    sSig := TJOSE.SerializeCompact(App.Engine.ApiConfig.GetSceretKey(ekUpbit, eaSpot),  TJOSEAlgorithmId.HS512, LToken);
    sToken := Format('Bearer %s', [sSig ]);

    with Req do
      Req.AddParameter( 'Authorization', sToken, TRESTRequestParameterKind.pkHTTPHEADER, [poDoNotEncode] );

    sRsrc := '/v1/withdraws/coin?'+sJson;
    Req.init(rmPOST, 'https://api.upbit.com', sRsrc, 'withdraw' )  ;

    Req.RequestAsync;
  finally
    LToken.Free;
  end;
end;


procedure TUpbitWithdraw.Parse(const aType, aData: string);
var
  aObj, aSub : TJsonObject;
  aArr  : TJsonArray;
  aErr, aVal : TJsonValue;
  aPair: TJsonPair;
  i, idx: Integer;
  stTmp : string;
  bOK : boolean;
  aItem : TDpItem;
begin
  if aDAta = '' then
  begin
    DoNotify( '출금 요청 실패', 999);
    Exit;
  end;

  if aType = 'status' then
  begin
    aObj := TJsonObject.ParseJSONValue( aData) as TJsonObject;
    try
      aErr  := aObj.GetValue('error');
      if aErr <> nil then
      begin
        DoNotify(aErr.GetValue<string>('message'), 999 );
        Exit;
      end;

      aSub  := aObj.GetValue('currency') as TJsonObject;
      if aSub = nil then
      begin
        DoNotify('출금 불가', 999 );
        Exit;
      end;

      bOK := false;
      aArr  := aSub.GetValue('wallet_support') as TJsonArray;
      for i := 0 to aArr.Count-1 do
      begin
        stTmp := aArr.Items[i].Value;
        if stTmp = 'withdraw' then
          bOK := true;
      end;

      if not bOK then
      begin
        DoNotify('출금 불가', 999 );
        Exit;
      end else
        ReuqestWithdraw;

    finally
      FreeAndNil(aObj);
    end;

  end else
  if aType = 'withdraw' then
  begin
    aObj := TJsonObject.ParseJSONValue( aData) as TJsonObject;
    try
      aErr  := aObj.GetValue('error');
      if aErr <> nil then
      begin
        DoNotify(aErr.GetValue<string>('message'), 999 );
        Exit;
      end;

      DoNotify('출금 성공');

      DoUpdateAsset;

    finally
      FreeAndNil(aObj);
    end;
  end else
  if aType = 'address' then
  begin
    aArr  := TJsonObject.ParseJSONValue( aData) as TJsonArray;
    try
      FAddress.Clear;

      aErr  := aObj.GetValue('error');
      if aErr <> nil then
      begin
        DoNotify(aErr.GetValue<string>('message'), 999 );
        Exit;
      end;

      for I := 0 to aArr.Count-1 do
      begin
        aVal  := aArr.Items[i] ;
        if (Req.Field1 = aVal.GetValue<string>('currency')) and ( Req.Field2 = aVal.GetValue<string>('net_type')) then
        begin
          stTmp := aVal.GetValue<string>('secondary_address', '');
          FAddress.Add(aVal.GetValue<string>('withdraw_address')+','+stTmp);
        end;
      end;

      DoNotify(aType);

    finally
      FreeAndNil(aArr);
      Req.Field1 := ''; Req.Field2 := '';
    end;
  end else
  if aType = 'deposit' then
  begin
    aVal  := TJsonObject.ParseJSONValue( aData);// as TJsonArray;
    if aVal = nil then Exit;

    try
      if not (aVal is TJsonArray) then
      begin
        aErr  := (aVal as TJsonObject).GetValue('error');
        if aErr <> nil then
          DoNotify(aErr.GetValue<string>('message'), 999 );
        Exit;
      end;

      aArr  := aVal as TJsonArray;

      idx := ifThen( Req.Field2 = 'first', 0, 1);
      if idx = 1 then
        FDpList[idx].Clear;

      for I := 0 to aArr.Count-1 do
      begin
        aVal  := aArr.Items[i] ;
        if ( (Req.Field1 = aVal.GetValue<string>('currency'))
          and (aVal.GetValue<string>('type') = 'deposit')
          and (aVal.GetValue<string>('state') = 'ACCEPTED')  ) then
        begin
          aItem := TDpItem.Create;
          aItem.Code  := Req.Field1;
          aItem.UUid  := aVal.GetValue<string>('uuid');
          aItem.Amount:= aVal.GetValue<string>('amount');
          aItem.Time  := GetStrToTime(aVal.GetValue<string>('done_at'));
          FDpList[idx].Add(aItem);
        end;
      end;

      if idx = 1 then
      begin
        bOK := CompareLists;
        if not bOK then
          DoNotify(aType);
      end;

    finally
      FreeAndNil(aVal);
      Req.Field1 := ''; Req.Field2 := '';
    end;
  end else
  if aType = 'depositaddr' then
  begin
    aObj := TJsonObject.ParseJSONValue( aData) as TJsonObject;
    try
      aErr  := aObj.GetValue('error');
      if aErr <> nil then
      begin
        DoNotify(aErr.GetValue<string>('message'), 999 );
        Exit;
      end;

      var bRes : boolean;

      if aObj.TryGetValue<boolean>('success', bRes) then
        DoNotify(RES_WT,  aObj.GetValue<string>('message') )
      else  begin
        var addr1, addr2 : string;
        addr1 := aObj.GetValue('deposit_address').Value;
        addr2 := aObj.GetValue('secondary_address').Value;
        if addr2 <> 'null' then
          addr1 := addr1 + '=' + addr2;
        DoNotify(addr1);
      end;
    finally
      FreeAndNil(aObj);
    end;
  end;
  ;

end;

function TUpbitWithdraw.CompareLists: Boolean;
var
  i: Integer;
  aItem : array [0..1] of TDpItem;
begin
  Result := False;

  // 리스트의 개수가 같은지 확인
  if FDpList[0].Count <> FDpList[1].Count then
    Exit;

  // 각 위치의 원소를 비교
  for i := 0 to FDpList[0].Count - 1 do
  begin
    // 원소가 다를 경우 두 리스트는 다름
    aItem[0] := TDpItem(FDpList[0].Items[i]);
    aItem[1] := TDpItem(FDpList[1].Items[i]);

    if aItem[0].UUid <> aItem[1].UUid then
      Exit;
  end;

  // 모든 원소가 같으면 두 리스트는 동일함
  Result := True;
end;

procedure TUpbitWithdraw.RecvRequest(Sender: TObject);
begin
  Parse( Req.Name, Req.Rsp.Content );
end;


end.
